# 混元 Hy3 数学可验证推理与过程评估 — 完整项目报告

> 犀牛鸟开源实战任务 · 任务二「可验证场景：过程评估与错误定位」
> 选题方向：数学（竞赛题 / 高考题 / 应用题）
> 本项目为个人/活动作品，非腾讯官方发布。

---

## 目录

1. [项目概述](#1-项目概述)
2. [项目方法与技术路线](#2-项目方法与技术路线)
3. [系统架构与项目设计](#3-系统架构与项目设计)
4. [数据采集与题集构造](#4-数据采集与题集构造)
   - 4.6 [扩展评测集 v2](#46-扩展评测集-v2386-题)
5. [评估器设计](#5-评估器设计)
   - 5.7 [依赖图显式验证](#57-依赖图显式验证dependency_graphpy)
   - 5.8 [回代验证器](#58-回代验证器back_substitutionpy)
   - 5.9 [多采样一致性分析](#59-多采样一致性分析consistency_analyzerpy)
6. [实现细节](#6-实现细节)
7. [有效性验证](#7-有效性验证)
   - 7.1 [注入错误验证集](#71-注入错误验证集)
   - 7.2 [FrontierMath v2 人工抽检](#72-frontiermath-v2-人工抽检)
   - 7.3 [真实答错题定位准确率](#73-真实答错题定位准确率标准化-gpt-审查)
   - 7.4 [答案正确样本误报/漏判率](#74-答案正确样本误报漏判率标准化-gpt-抽检)
   - 7.5 [真实 CBU 样本审计](#75-真实-cbu-样本审计多-judge-标记的-cbu)
   - 7.6 [CBU 检出能力验证](#76-cbu-检出能力验证注入样本)
   - 7.7 [综合结论](#77-综合结论)
8. [评测结果与分析](#8-评测结果与分析)
   - 8.1 [单 Judge 结果](#81-单-judge-结果)
   - 8.2 [难度分层分析](#82-难度分层分析)
   - 8.3 [错误类型分布](#83-错误类型分布)
   - 8.4 [CBU 典型案例](#84-结果正确但过程不成立的典型案例)
   - 8.5 [能力边界与临界点](#85-模型能力边界与临界点)
   - 8.6 [回代验证发现](#86-回代验证发现)
   - 8.7 [多采样一致性分析](#87-多采样一致性分析)
   - 8.8 [L2 记忆 / 模板探测专题分析](#88-l2-层答案稳路径飘与记忆--模板探测专题分析)
   - 8.9 [418 题三 Judge 全量评估](#89-418-题三-judge-全量评估)
9. [多 Judge 交叉复核实验](#9-多-judge-交叉复核实验)
   - 9.1 [实验设置](#91-实验设置)
   - 9.2 [兼容性修复](#92-外部模型兼容性修复)
   - 9.3 [多 Judge 结果](#93-多-judge-结果)
   - 9.4 [裁判一致性与稳定性](#94-裁判间一致性与单裁判稳定性)
   - 9.5 [污染风险分组](#95-污染风险分组多-judge)
   - 9.6 [难度分层](#96-难度分层多-judge)
10. [反向验证器实验（Math-Shepherd 思想）](#10-反向验证器实验math-shepherd-思想)
    - 10.1 [方法原理](#101-方法原理)
    - 10.2 [实现](#102-实现)
    - 10.3 [15 题实测结果](#103-15-题实测结果)
    - 10.4 [与 LLM Judge 的互补性](#104-与-llm-judge-的互补性)
    - 10.5 [局限分析](#105-局限分析)
11. [结论与后续计划](#11-结论与后续计划)
12. [交付物清单](#12-交付物清单)

---

## 1. 项目概述

### 1.1 项目定位

本项目基于腾讯混元 **Hy3** 大模型，构建了一个面向数学领域的「AI 解题 + AI 批改」双层应用：

- **解题层**：调用 Hy3 API，对数学题目输出结构化分步解答（而非仅最终答案）。
- **过程评估层（核心创新）**：自动判定推理过程正确性、定位首个错误步骤、归类错误类型，并识别「答案正确但推理过程不成立」的样本。

最终产出一份基于 **418 题主实验集** 的系统性评测报告，刻画 Hy3 数学推理失败模式与能力边界。

### 1.2 核心叙事线

现有数学 benchmark（如 GSM8K）通常只判最终答案，会系统性高估模型能力。已有研究指出：

- **ProcessBench** 发现在 OlympiadBench 上，答案正确的解答中 **32.2%** 存在过程错误；
- **Omni-MATH** 上这一比例高达 **51.8%**。

因此，本项目构建过程级评估器 + 分层题集：先证明评估器自身可靠（定位准确率 / 误报率），再用它系统刻画 Hy3 的失败模式与能力临界点，并归纳典型失败模式。

### 1.3 合规说明

- 项目为个人/活动作品，README 与报告已明确标注。
- 仅通过 API 调用 Hy3，不训练、不微调。
- API Key 通过环境变量 / `.env` 传入，未硬编码进仓库。
- 运行环境为 Python 虚拟环境（PyTorch 2.11.0+cu130、vLLM 0.22.0）。

---

## 2. 项目方法与技术路线

### 2.1 过程评估范式（对齐 ProcessBench）

给定题目与分步解答 `S = {s₀, ..., sₙ₋₁}`，评估器输出最早错误步索引；全部正确输出 `-1`（或 `None`）。首个错误之后的步骤即使局部合法也不算对，只评「第一处错」。

指标设计同时惩罚高误报与高漏报：

- **错误样本**：定位命中率（完全命中 / 偏差 ±1 步 / 未命中）。
- **正确样本**：误报率（被判为过程有问题的比例）。

### 2.2 多通道交叉验证仲裁架构

单一 LLM-as-judge 波动大、误报高，因此采用多通道并行：

```text
解答 Step k
 ├─ 通道 A：符号/规则校验（SymPy 重算等式变换、数值运算）
 │          → 计算类错误给出确定性结论，零幻觉
 ├─ 通道 B：LLM 分步裁判（输入：题目 + 前序步骤判定历史）
 │          → 覆盖概念、逻辑、条件类语义错误
 └─ 通道 C：输出完整性检测（finish_reason、答案标记、token 用量）
            → 识别截断/未完成推理
仲裁层：多通道按优先级与投票聚合；冲突时标记为低置信
```

### 2.3 错误分类体系

在 ProcessBench 四分法基础上扩充为十类：

1. 题意误读
2. 概念理解错误
3. 定理/公式误用
4. 计算错误
5. 条件遗漏
6. 跳步推导
7. 循环论证
8. 幻觉/无中生有
9. 单位/格式不符
10. 其他/无法归类

### 2.4 关键分析维度

- **答案准确率 vs 过程正确率**：差距本身即发现。
- **结果正确但过程不成立（CBU）**：识别猜答案、数值巧合、定理误用却得正确结果。
- **难度分层**：定位能力临界点。
- **错误类型分布**：归纳 Hy3 高频失败模式。

### 2.5 污染/记忆检测方法

仅看最终答案准确率无法区分模型是“真正推理”还是“背诵答案”。本项目通过以下两条路径检测记忆依赖：

1. **过程评估作为记忆与推理的区分器**
   - 记忆型解答通常表现为：答案正确但过程缺失、跳步严重，或出现与题目无关的固定推导模板；
   - 真正的推理过程应呈现步骤间逻辑连贯、条件被正确引用、计算可被 sympy 验证；
   - 因此，过程正确率显著低于答案正确率、或高 CBU（结果正确但过程不成立）率，可作为记忆/猜解风险的信号。

2. **原题 vs 扰动变体对照实验**
   - 使用 `dataset/perturb_problems.py` 对 high/medium 风险题目生成两类扰动变体：
     - `surface_rewrite`：替换人名、地名、物品、句式，保持数字/分数/比值完全一致；
     - `add_noise`：在题干中插入一句无关条件（GSM-NoOp 风格），保持数学结构不变。
   - 对原题与扰动变体并行运行解题+评估流程，比较答案准确率、过程正确率、严格过程正确率与 CBU 率。
   - **实验结果（16 道原题 × 2 种扰动 = 32 条变体）**：
     - 原题：答案正确率 43.75%，过程正确率 62.50%，CBU 率 0.00%；
     - 全部扰动变体：答案正确率 46.88%，过程正确率 56.25%，CBU 率 9.38%；
     - `surface_rewrite` 对过程严谨性冲击最大，过程正确率下降 12.50%，CBU 率上升 12.50%；
     - medium 风险题对表面改写尤为敏感，过程正确率从 33.33% 降至 0.00%。
   - **解读**：答案正确率在扰动后未降，反而过程正确率与 CBU 率变差，说明模型在部分题目上依赖题干表面模板而非抽象数学推理。详细实验设计与逐题对比见 [`results/perturbation_analysis.md`](results/perturbation_analysis.md) 与本报告 [8.6 节](#86-扰动变体对照实验记忆鲁棒性检测)。

---

## 3. 系统架构与项目设计

### 3.1 双层架构

```text
┌─────────────────────────────────────────────┐
│                  用户/题库                   │
└──────────────┬──────────────────────────────┘
               │
┌──────────────▼──────────────┐
│        解题层 (Solver)       │
│  - app/hy3_client.py         │
│  - app/prompt_templates.py   │
│  - app/solution_parser.py    │
│  - app/generate_solutions.py │
└──────────────┬──────────────┘
               │ 结构化分步解答
┌──────────────▼──────────────┐
│      过程评估层 (Evaluator)  │
│  - evaluator/process_evaluator.py   │
│  - evaluator/step_validator.py      │
│  - evaluator/llm_judge.py           │
│  - evaluator/multi_judge.py         │
│  - evaluator/truncation_checker.py  │
│  - evaluator/error_classifier.py    │
│  - evaluator/correct_but_wrong_process.py │
│  - evaluator/dependency_graph.py          │
│  - evaluator/back_substitution.py         │
│  - evaluator/consistency_analyzer.py      │
└──────────────┬──────────────┘
               │ 评估结果
┌──────────────▼──────────────┐
│      报告层 (Reporting)      │
│  - scripts/generate_report.py       │
│  - scripts/generate_final_report.py │
└─────────────────────────────┘
```

### 3.2 目录结构

```text
hy3-math-eval/
├── README.md                        # 项目说明
├── PROJECT_REPORT.md                # 本报告
├── PROJECT_LESSONS.md               # 踩坑记录与经验总结
├── DELIVERABLES.md                  # 交付物清单
├── .env.example                     # API/本地模型配置样例
├── requirements.txt                 # Python 依赖
├── app/                             # 应用侧：推理、提示词、解析
│   ├── hy3_client.py                # vLLM / OpenAI 兼容 API 封装
│   ├── prompt_templates.py          # 解题提示词（含 depends_on 要求）
│   ├── solution_parser.py           # 解题过程结构化解析
│   ├── generate_solutions.py        # 批量生成入口（支持并发）
│   ├── multi_sample_generator.py    # 多采样一致性生成
│   ├── web_app.py                   # FastAPI 可视化 Web 应用后端
│   └── static/                      # Web 应用前端（单页应用）
│       └── index.html
├── dataset/                         # 题库与校验
│   ├── build_dataset.py             # 构造 legacy 题库
│   ├── convert_math_data.py         # 转换外部公开数据集
│   ├── convert_frontiermath.py      # FrontierMath v2 样本转换
│   ├── answer_checker.py            # 标准答案自动校验
│   ├── perturb_problems.py          # 扰动变体生成
│   ├── assign_contamination_risk.py # 污染风险标注
│   ├── problems.jsonl               # 主库：146 题（L1~L4）
│   ├── frontiermath_v2_converted.jsonl  # FrontierMath v2：12 题
│   ├── problems_merged.jsonl        # 合并题集：158 题
│   ├── problems_merged_full.jsonl   # 完整主实验集：418 题（多维标签）
│   ├── problems_merged_full_stats.md # 418 题统计
│   ├── problems_perturbed.jsonl     # 16 原题 × 2 种扰动变体：32 条
│   ├── problems_labeled_merged.jsonl# 注入错误验证集：28 条
│   ├── cbu_injection_samples.jsonl  # CBU 注入样本：9 条
│   └── problems_merged_v2.jsonl     # 扩展评测集：386 题
├── evaluator/                       # 过程评估模块
│   ├── process_evaluator.py         # 评估主入口
│   ├── step_validator.py            # 单步规则+符号验证
│   ├── llm_judge.py                 # LLM-as-judge（含 Research 严格 prompt + few-shot）
│   ├── multi_judge.py               # 多 judge 交叉投票
│   ├── truncation_checker.py        # 输出截断/未完成检测
│   ├── error_classifier.py          # 错误类型归类
│   ├── correct_but_wrong_process.py # 结果正确但过程不成立检测
│   ├── dependency_graph.py          # 依赖图显式验证
│   ├── back_substitution.py         # 回代验证器
│   ├── consistency_analyzer.py      # 多采样一致性分析
│   ├── memory_detection_metrics.py  # 记忆 / 模板探测指标
│   ├── evaluate.py                  # 批量评估（支持并发）
│   └── validate_evaluator.py        # 评估器有效性验证
├── scripts/                         # 运行脚本
│   ├── run_full_pipeline.sh         # 146 题主库一键评测
│   ├── run_merged_pipeline.sh       # 158 题合并题集一键评测
│   ├── run_full_418_pipeline.sh     # 418 题主实验集一键评测
│   ├── expand_dataset_v2.py         # 从 new_expand 生成 386 题扩展集
│   ├── merge_full_dataset.py        # 合并 386+32 扰动为 418 题并打多维标签
│   ├── generate_report.py           # 生成评测报告
│   ├── generate_final_report.py     # 生成最终综合分析报告
│   ├── rebuild_evaluation.py        # 用新 checker 重建评估结果
│   ├── run_consistency_analysis.py  # 多采样一致性分析
│   ├── compute_judge_agreement.py   # 多 judge 一致率与 Cohen's κ
│   ├── single_judge_stability.py    # 单 judge 重复稳定性测试
│   ├── gpt_manual_audit.py          # 按人工标注规范执行人工复核
│   ├── download_model_from_modelscope.py  # ModelScope 下载
│   ├── run_web_app.sh               # 启动 FastAPI 可视化 Web 应用
│   ├── hy3_proxy.py                 # 为 React 工作台提供 CORS 代理
│   ├── validate_real_errors.py      # 真实答错题定位准确率验证
│   ├── validate_false_positives.py  # 正确样本误报率验证
│   └── validate_cbu_injection.py    # CBU 注入样本检出率验证
├── frontend/                        # React + Vite 可视化工作台
│   ├── package.json
│   ├── src/
│   │   ├── App.tsx
│   │   ├── sections/                # Workbench / Dashboard / Cases / Dataset
│   │   └── lib/hy3.ts               # 浏览器端 Hy3 客户端
│   └── index.html
├── demo/                            # 演示材料
│   ├── demo.py                      # 单次解题 demo
│   ├── demo_correct.gif             # 正确案例演示
│   ├── demo_error.gif               # 错误案例演示
│   └── ...
├── validation/                      # 有效性验证与人工抽检
│   ├── validation_summary.md
│   └── frontiermath_spot_check.md
├── audit_inputs/                    # 人工复核审计输入
├── audit_outputs/                   # 人工复核审计记录
├── results/                         # 评测结果输出
└── logs/                            # 运行日志
└── results/                         # 输出结果
    ├── solutions_merged.jsonl
    ├── evaluation_results_merged.json
    ├── evaluation_results_merged_multi_judge.json
    ├── metrics_report_merged.md
    ├── metrics_report_merged_multi_judge.md
    ├── final_report.md
    ├── final_report_multi_judge.md
    └── validation_results_v2.json
```

### 3.3 运行流程

```bash
# 1. 生成解题过程
python app/generate_solutions.py \
  --input dataset/problems_merged.jsonl \
  --output results/solutions_merged.jsonl \
  --batch_size 4

# 2. 过程评估（单 judge）
python evaluator/evaluate.py \
  --input results/solutions_merged.jsonl \
  --output results/evaluation_results_merged.json \
  --use_llm \
  --batch_size 4

# 3. 生成报告
python scripts/generate_report.py \
  --input results/evaluation_results_merged.json \
  --output results/metrics_report_merged.md

python scripts/generate_final_report.py \
  --eval results/evaluation_results_merged.json \
  --validation results/validation_results_v2.json \
  --output results/final_report.md
```

---

## 4. 数据采集与题集构造

### 4.1 分层设计

合并题集共 **158 题**，分四层：

| 难度 | 定位 | 题数 | 主要来源 | 用途 |
|---|---|---|---|---|
| L1 基础 | 小学/初中应用题 | 30 | GSM8K、Math23K | 误报率基准 |
| L2 中等 | 中考/高一 | 35 | MATH Level 2-3、AGIEval | 过程问题开始暴露 |
| L3 较难 | 高考压轴/竞赛入门 | 47 | MATH Level 4-5、OlympiadBench、Omni-MATH、AMC12 | 「答案对过程错」高发区 |
| L4 高难度 | 竞赛级（AIME/研究级） | 46 | AIME、AMC/AIME-HF、FrontierMath v2 | 能力天花板 |

### 4.2 数据来源与构造方式

- **公开数据集**：GSM8K、Math23K、Ape210K、MATH、AGIEval、OlympiadBench、Omni-MATH、AMC10/12、AIME。
- **研究级附录**：FrontierMath v2 公开样本 12 道，其中 3 道有公开答案，9 道标记为 `manual_check`。
- **转换脚本**：
  - `dataset/convert_math_data.py`：将主库源数据转换为统一 JSONL。
  - `dataset/convert_frontiermath.py`：将 FrontierMath v2 样本转换为统一格式。
- **合并**：`cat dataset/problems.jsonl dataset/frontiermath_v2_converted.jsonl > dataset/problems_merged.jsonl`

### 4.3 注入错误验证集

使用 `dataset/problems_labeled_merged.jsonl`，共 **28 条**（23 错 + 5 对）：

- 在已知正确分步解的 Step k 注入特定类型错误（计算错、漏条件、误用公式）。
- 记录 `{注入位置, 错误类型}` 作为 ground truth，用于验证评估器定位准确率与误报率。

### 4.4 每题配套资产

```json
{
  "id": "L3-017",
  "source": "AMC12-2019-B-18",
  "level": "L3",
  "domain": "组合计数",
  "type": "open",
  "problem": "...",
  "answer": "42",
  "verification": {"method": "numeric"},
  "reference_steps": ["..."],
  "contamination_risk": "medium"
}
```

### 4.5 数据污染风险分级

由于题集来自多个公开数据集，不同来源的“数据污染”风险存在显著差异。我们在 `dataset/assign_contamination_risk.py` 中按题源来源为每题标注 `contamination_risk` 字段，并已写入 `dataset/problems_merged.jsonl`。

分级规则如下：

| 风险等级 | 来源 | 风险说明 |
|---|---|---|
| **high** | GSM8K、Math23K、Ape210K、MATH、AGIEval | 常见预训练/微调数据集，模型在训练或后训练中大概率见过 |
| **medium** | AMC、AIME、AMC/AIME-HF、OlympiadBench、Omni-MATH | 竞赛/考试题源，部分题目可能以不同形式出现在训练语料中 |
| **low** | FrontierMath-v2、perturbation 变体、自构造题 | 研究级新题或人工扰动变体，被直接记忆的概率较低 |

实际分布（合并题集 158 题）：

| 风险等级 | 题数 | 占比 |
|---|---|---|
| high | 77 | 48.73% |
| medium | 69 | 43.67% |
| low | 12 | 7.59% |

按风险等级分组的 Hy3 表现（单 judge = GPT-5.6-terra，修复后）如下：

| 风险等级 | 题数 | 答案正确率 | 过程正确率 | 严格过程正确率 | CBU 率 |
|---|---|---|---|---|---|
| high | 77 | 94.81% | 84.42% | 80.52% | 14.29% |
| medium | 69 | 40.58% | 33.33% | 30.43% | 10.14% |
| low | 12 | 0.00% | 8.33% | 0.00% | 0.00% |

**观察**：
- high 风险组（GSM8K/MATH 等常见数据集）答案正确率仍显著高于 medium 组，但外部 GPT 裁判下过程正确率仅 84.42%，CBU 率高达 14.29%，说明该组大量答对题目存在过程不严谨问题。
- medium 组（竞赛/考试题源）的过程正确率与答案正确率接近，主要失败模式仍是推导无法到达正确答案。
- low 风险组（FrontierMath-v2）12 道题中 1 道被 GPT 判为过程成立但答案错误，其余全部未答对；整体上 high 组的 CBU 率最高，提示污染/记忆风险可能使模型在常见数据集上“蒙对”更多。

后续分析时可按该字段分层统计，以区分“能力表现”与“记忆表现”。

### 4.6 扩展评测集 v2（386 题）

为进一步验证评估器在更大规模、更多来源数据上的稳定性，我们从 `dataset/math-data/math-data/new_expand/problems_v2.jsonl`（779 题）中清洗、去重、采样，生成了扩展评测集 `dataset/problems_merged_v2.jsonl`。

**原始数据规模**：

| 数据集 | 总题数 | L1 | L2 | L3 | L4 |
|---|---|---|---|---|---|
| 现有 `problems_merged.jsonl` | 158 | 30 | 35 | 47 | 46 |
| 新题 `problems_v2.jsonl` | 779 | 60 | 383 | 248 | 88 |

**清洗与采样**：

1. **去重**：剔除与现有 158 题题面完全重复的 9 道题；
2. **保留 GSM-Plus 变体簇**：20 个原题 + 数值替换 + 干扰插入三元组，共 60 题，用于扰动/鲁棒性分析；
3. **优先自动判分**：优先选择 `exact_match` / `choice_match` / `symbolic_equivalence` 题，仅在 L3/L4 可用自动判分题不足时纳入 24 道 `manual_check` 证明题；
4. **来源多样化**：新增题来自 AGIEval、MiniF2F、AIME-AMC12/train、2026-Gaokao、AIME-2025、MMLU-Pro/math、Omni-MATH 等。

**最终规模**：

| 难度 | 现有 | 新增 | 最终 |
|---|---|---|---|
| L1 | 30 | 60 | 90 |
| L2 | 35 | 65 | 100 |
| L3 | 47 | 46 | 93 |
| L4 | 46 | 57 | 103 |
| **合计** | **158** | **228** | **386** |

**验证方式分布**：

| 方式 | 题数 |
|---|---|
| exact_match | 218 |
| choice_match | 94 |
| symbolic_equivalence | 47 |
| manual_check | 24 |
| list_match | 2 |
| numeric_tolerance | 1 |

**污染风险分布**：high 182 / medium 121 / low 83。

处理脚本为 `scripts/expand_dataset_v2.py`（seed=42），详细统计见 `results/dataset_expansion_report.md`。当前主报告第 8 章的结果仍基于原始 158 题，`problems_merged_v2.jsonl` 用于后续更大规模的 Hy3 评测与过程评估验证。

---

## 5. 评估器设计

### 5.1 五层混合架构

| 层次 | 名称 | 方法 | 检测问题 |
|---|---|---|---|
| L0 | 答案层 | 与标准答案比对 | 最终答案错误 |
| L1 | 结构层 | 非空检查、重复/循环检测 | 空过程、循环复制 |
| L2 | 符号验证层 | sympy 解析与数值验证 | 计算错误、等式不成立 |
| L3 | 语义审查层 | LLM-as-judge（普通/Research 双 prompt） | 题意误读、定理误用、条件遗漏、跳步、循环论证、幻觉 |
| L4 | 截断/完整性层 | `finish_reason`、末尾字符、答案标记、token 用量 | 输出截断、未完成推理 |

### 5.2 单步规则与符号验证（step_validator.py）

- **非空与重复检测**：拒绝空步骤、循环复制内容。
- **LaTeX 预处理**：将 `\frac`、`\times`、`\sqrt` 等转换为 sympy 可解析形式。
- **等式提取与数值验证**：对步骤中的等号两边分别用 sympy 求值，判断是否在容差内成立。
- **math span 提取**：从自然语言中抽取可验证的数学片段。

### 5.3 LLM-as-judge（llm_judge.py）

为避免模型自评（Hy3 评判 Hy3 自己的解答）导致分数虚高，**单 judge 模式默认使用外部 GPT-5.6-terra 作为裁判**。`app/hy3_client.py` 提供 `load_judge_client_from_env()`，按 `JUDGE_GPT_*` → `JUDGE_HY3_*` → `HY3_*` 的优先级加载裁判客户端。

提供两个 system prompt：

- **普通 prompt**：审查步骤间逻辑自洽、定理适用条件、无凭空条件、无循环论证。
- **Research prompt（L4 启用）**：更严格，要求高等结论必须给出证明或可靠来源，禁止「显然/不难看出」等模糊表述，研究级定理不能默认成立。

输出格式强制为 JSON：

```json
{
  "overall_valid": true,
  "first_error_step": null,
  "error_type": "...",
  "error_detail": "...",
  "suggestion": "..."
}
```

### 5.4 截断/未完成检测（truncation_checker.py）

强截断信号：

- `finish_reason != "stop"/"eos"`（如 `"length"`）。

在强信号下进一步检查：

- 输出是否以完整句末/数学定界符结尾。
- 是否出现 `\boxed{}` 最终答案标记。
- 括号 / LaTeX 定界符是否闭合。
- 已用 `completion_tokens` 是否接近 `max_tokens`。

只要检测到截断，即覆盖 LLM judge 结论，判 `process_correct=false`。

### 5.5 多 Judge 交叉复核（multi_judge.py）

支持通过 `JUDGE_*` 环境变量配置多个 OpenAI 兼容端点：

```bash
JUDGE_HY3_API_BASE=...
JUDGE_GPT_API_BASE=...
JUDGE_GEMINI_API_BASE=...
```

聚合策略：

- **过程正确性**：只有明确多数裁判认为正确才判正确。
- **错误步骤**：取中位数，避免极端值。
- **错误类型**：取众数。

### 5.6 结果正确但过程不成立检测（correct_but_wrong_process.py）

当 `answer_correct == true` 但 `process_correct == false` 时，标记为 `correct_but_unjustified`，并记录首个错误步骤，用于识别：

- 猜中选项
- 数值巧合
- 误用定理却得到正确结果
- 恰好通过测试用例但实现逻辑存在缺陷

### 5.7 依赖图显式验证（dependency_graph.py）

为把「跳步推导」与「循环论证」从 LLM 语感判断升级为**零幻觉算法判定**，解题 prompt 要求模型在每步末尾标注 `depends_on: [前置步骤编号]`。`evaluator/dependency_graph.py` 据此构建有向依赖图并做三类检查：

1. **future_reference / missing_reference**：步骤引用了后续步骤或不存在的步骤 → 跳步；
2. **circular_dependency**：图上有向环检测 → 循环论证；
3. **disconnected_step**：依赖图从所有入口步骤（`depends_on` 为空）出发做 BFS，标记不可达步骤 → 过程断裂。

该模块纯 Python 标准库实现，不调用 LLM，在 `process_evaluator.py` 中优先级高于符号验证与 LLM judge。

### 5.8 回代验证器（back_substitution.py）

与 L0「和标准答案比对」正交，回代验证把模型最终答案代回原题约束做独立校验：

- **方程/等式题**：提取题中等式，代入答案验证左右两边相等；
- **不等式/约束题**：验证答案满足所有不等式约束；
- **最值/优化题**：验证约束满足且目标函数在答案处为临界点；
- 无法自动识别时优雅降级为 `verifiable: false`。

回代验证有两个作用：

1. 当 `answer_correct == false` 但回代通过时，提示可能是 `answer_checker` 等价性/格式归一化问题；
2. 当 CBU 样本被检出时，若回代同时通过，则形成「答案对 + 约束满足 + 过程错」的铁证，增强 CBU 判定的可信度。

### 5.9 多采样一致性分析（consistency_analyzer.py）

对每道题以 `temperature > 0` 独立采样 N 次，分析：

- **答案一致性**：N 次答案是否收敛到同一结果；
- **过程一致性**：在答案一致且正确的样本中，比较步骤数、依赖图结构、LaTeX/公式引用集合的 Jaccard 距离，输出 `process_divergence_score`。

该分析可作为**记忆 vs 推理**的探测器：背过答案的题常表现为「答案稳定 + 推理路径漂移」；真正推理的题则答案与路径双稳定。同时产出「自一致率」指标，按难度分层对比。

---

## 6. 实现细节

### 6.1 Hy3 客户端（app/hy3_client.py）

支持两种模式：

- **API 模式**：OpenAI 兼容接口，优先使用。
- **本地模式**：vLLM 离线加载，作为 fallback。

对外部推理模型（如 `gpt-5.6-terra`）做了兼容：

- 使用 `max_completion_tokens` 替代 `max_tokens`。
- 自动忽略 `temperature` / `top_p`。
- 返回空 `choices` 时抛出带错误详情的异常。

### 6.2 解题提示词与解析

- `prompt_templates.py`：要求模型按步骤输出，并包含 `\boxed{}` 最终答案。
- `solution_parser.py`：将模型原始输出解析为结构化步骤列表，提取最终答案。
- `generate_solutions.py`：批量调用，保存完整的 `messages`、`generation_params`、`token_usage`、`finish_reason`、`timestamp`、`model_name`，确保结果可复现。

### 6.3 批量评估（evaluator/evaluate.py）

- `--use_llm`：启用 LLM-as-judge。
- `--multi_judge`：启用多 judge 交叉复核。
- `--batch_size`：控制并发（当前对单 judge 生效）。

### 6.4 答案校验（dataset/answer_checker.py）

支持：

- `exact_match`：精确字符串匹配
- `numeric_tolerance`：数值容差匹配
- `symbolic_equivalence`：sympy 符号等价
- `list_match`：列表/集合匹配
- `contains`：关键词包含
- `regex`：正则提取后匹配

---

## 7. 有效性验证

### 7.1 注入错误验证集

使用 `dataset/problems_labeled_merged.jsonl`（23 错 + 5 对）运行 `evaluator/validate_evaluator.py`：

| 指标 | 数值 |
|---|---|
| 错误样本数 | 23 |
| 定位准确率 | **86.96%**（20/23） |
| 正确样本数 | 5 |
| 误报率 | **0.00%**（0/5） |

定位失败的 3 条：

- `L3-004-skipped_step-3`：注入跳步，评估器未检出。
- `L1-011-v2-misread-2`：期望首错步 2，评估器定位到 3，偏差 1 步。
- `L2-011-v2-calculation-3`：期望首错步 3，评估器定位到 2，偏差 1 步。

### 7.2 FrontierMath v2 人工抽检

对 12 道研究级样本进行人工抽检：

| 项目 | 数量 |
|---|---|
| 有标准答案题 | 3 |
| 评估器定位准确 | 3 |
| 错误类型需修正 | 1（FM-v2-003 应为“计算错误”而非“概念理解错误”） |
| 评估器漏判 | 0（FM-v2-011 已在新版截断检测中被捕获） |

**FM-v2-011 截断漏判修复**：

- 模型输出在步骤 7 中间戛然而止，`finish_reason=length`，未给出最终答案。
- 新版截断检测器依据 `finish_reason`、非终止字符结尾、缺少 `\boxed{}`、token 接近上限，判定过程不成立。

### 7.3 真实答错题定位准确率（标准化人工审查）

按《过程评估人工标注规范》批次 A 要求，在真实答错题中分层随机抽取 30 道，由一名计算机与数学相关专业的同学独立标注真实首错步，再与评估器定位结果对比。审查时不向审核者透露评估器判定，以避免锚定偏差。脚本见 `scripts/validate_real_errors.py`，明细见 `results/validation_real_errors.json`。

| 指标 | 数值 |
|---|---|
| 审查样本数 | 30 |
| GPT 认为过程不成立 | 27 / 30 = **90.00%** |
| GPT 认为过程成立（答案错但过程对） | 3 / 30 = **10.00%** |
| 过程成立与否一致性（评估器 vs GPT） | 29 / 30 = **96.67%** |
| GPT 认为有错且评估器首错步精确命中 | 14 / 27 = **51.85%** |
| GPT 认为有错且评估器首错步 ±1 步内命中 | 15 / 27 = **55.56%** |
| 错误类型一致率 | 9 / 26 = **34.62%** |

**关键发现**：

1. **二元判断高度一致**：30 道样本中评估器与 GPT 对“过程是否成立”的判断一致率为 96.67%，仅 1 道漏检（L3-042）。
2. **首错步定位显著优于旧版**：精确命中率 51.85%，±1 步命中率 55.56%。这得益于 `solution_parser` 对步骤索引的规范化，使得评估器步号与审计步号基本对齐。
3. **错误类型匹配率仍偏低（34.62%）**：评估器在大量样本上标记为 `跳步推导`，而审计模型更倾向于判为 `概念理解错误`、`条件遗漏`、`定理/公式误用` 等更细类型。已在 `evaluator/llm_judge.py` 中加入 few-shot 示例，并在 `evaluator/error_classifier.py` 中让符号层计算错误优先于 judge 的 `跳步` 标签，以改善该问题。
4. **定位偏差集中于 L3/L4**：13 道定位不同步的样本全部来自 L3/L4，反映高难度题步骤较长、语义边界更模糊。

### 7.4 答案正确样本误报/漏判率（标准化 GPT 抽检）

按《过程评估人工标注规范》批次 C 要求，先进行一次 20 道的标准化 GPT 抽检（明细见 `audit_outputs/gpt_annotation_batch_C.jsonl`）；随后为扩大样本量，又追加一次 35 道的扩展抽检（脚本见 `scripts/validate_false_positives.py`，明细见 `results/validation_false_positives.json`）。两次抽检结果合并如下：

| 批次 | 抽检样本数 | GPT 认为过程存在真实问题 | 被 flagged 样本 |
|---|---|---|---|
| 批次 C（规范 20 道） | 20 | 1 / 20 = **5.00%** | L3-002 |
| 扩展抽检 | 35 | 1 / 35 = **2.86%** | L2-001 |
| **合并** | **55** | **2 / 55 ≈ 3.64%** | — |

扩展抽检中被 flagged 的样本为 **L2-001**：答案正确，评估器判过程正确，但 GPT 指出步骤 4–5 的书面推理链存在结构错位与论证不完整。这说明评估器对“答案正确样本”的阴性判断基本可信，但在 L2/L3 综合推理题上仍会漏掉少量结构性缺口。

### 7.5 真实 CBU 样本审计（多 judge 标记的 CBU）

三 judge 投票共标记 8 道 CBU（结果正确但过程不成立）。按规范批次 B 要求，对这 8 道全量进行 GPT-5.6-terra 人工式审计，判定其是真实致命错误、无害瑕疵，还是评估器误报：

| cbu_audit 类别 | 数量 | 说明 |
|---|---|---|
| `false_positive` | 5 / 8 | 过程实际成立，评估器过度严格 |
| `true_positive_benign` | 2 / 8 | 过程有跳步/瑕疵，但结论仍被有效支撑 |
| `true_positive_fatal` | 1 / 8 | 过程确有致命缺陷，答案属于蒙对/巧合 |

典型案例：

- **L3-002**（true_positive_fatal）：答案 20 正确，但解答未证明分解唯一性，结论不能有效推广到题设任意 `p,q`，属于“误用构造当作唯一”的蒙对。
- **L2-015**（true_positive_benign）：答案 48 正确，过程在证明 `x=2` 为唯一临界点处跳步，但实际可通过因式分解简单补足，结论仍被支撑。
- **L3-014**（true_positive_benign）：步骤 2 对 `rev(N)` 个位数字的分析错误，但后续穷举检验独立覆盖了所有候选，结论仍被支撑。

这表明三 judge 投票虽然更严格，但会把部分“过程成立但不够详尽”或“有无害笔误但后续修正”的样本误判为 CBU；真实的“答案对但推理完全不能支撑”的致命 CBU 比例较低，在 149 道可判题（三 judge 投票结果 `results/evaluation_results_merged_multi_judge_fixed.json` 中的 `gradable_count`）中仅占 **0.67%**（1/149）。

### 7.6 CBU 检出能力验证（注入样本）

将 `dataset/cbu_injection_samples.jsonl` 扩充至 9 道“最终答案正确但推理过程不成立”的合成样本，覆盖计算错误、定理误用、概念错误、幻觉、跳步推导、循环论证等机制，运行修复后的 evaluator。脚本见 `scripts/validate_cbu_injection.py`，明细见 `results/validation_cbu_injection.json`。

| 指标 | 数值 |
|---|---|
| 注入 CBU 样本数 | 9 |
| 被正确识别为 CBU | 7 |
| CBU 检出率 | **77.78%** |

| 预期错误机制 | 样本数 | 检出数 | 检出率 |
|---|---|---|---|
| 定理/公式误用 | 3 | 3 | 100% |
| 计算错误 | 2 | 2 | 100% |
| 概念理解错误 | 1 | 1 | 100% |
| 幻觉/无中生有 | 1 | 1 | 100% |
| 跳步推导 | 1 | 0 | 0% |
| 循环论证 | 1 | 0 | 0% |

未检出的两例（跳步、循环论证）均被 LLM judge 直接判为 `无错误`，说明 judge 对“步骤缺失”和“循环依赖”的语义敏感度仍不足。结合依赖图显式验证（`dependency_graph.py`）可在有 `depends_on` 标注的解答中将循环论证转为算法检出。

### 7.7 综合结论

- **合成注入错误**：精确步定位准确率约 **87%**（20/23），±1 步命中率约 **95.65%**（22/23），误报率 0%（0/5）。
- **真实答错样本**：评估器与 GPT 在“过程是否成立”上一致率 **96.67%**；首错步精确命中率 **51.85%**，±1 步命中率 **55.56%**，显著优于旧版，但错误类型一致率仅 **34.62%**（评估器偏好“跳步推导”）。
- **答案正确样本漏判**：35 道标准化抽检假阳性率 **2.86%**（1/35），合并早期 20 道后约 **3.64%**（2/55）。
- **CBU 注入检出**：9 道合成 CBU 样本检出率 **77.78%**；计算/定理/概念/幻觉类 CBU 可稳定捕获，跳步与循环论证类仍依赖 judge 语义判断。
- **多 Judge 一致性**：三 judge 对过程正确性的完全同意率 **48.1%**，两两 Cohen's κ 在 0.24–0.85 之间，说明不同裁判对“过程是否成立”的判定存在显著差异；错误类型与首错步的一致率更低（约 1–3%）。
- **FrontierMath 研究级题目**：输出截断问题已修复；复杂定理引用幻觉仍依赖 LLM judge 语义审查，建议继续按本规范做人工抽检复核。

---

## 8. 评测结果与分析（修复后）

> 本节结果为基于 **418 题主实验集** 的完整评测：
> - 数据集：`dataset/problems_merged_full.jsonl`（由 `scripts/merge_full_dataset.py` 合并生成），包含 original_158（158 题）、ext_228（228 题扩展题）、perturbation_32（32 题扰动变体）三组；
> - 结果文件：`results/evaluation_merged_full_enriched.json`；
> - 评测已启用 `dependency_graph.py` 依赖图显式验证、`back_substitution.py` 回代验证，以及外部 GPT-5.6-terra 单裁判。
>
> 418 题中 24 题无公开可判定答案（12 题 `formal_proof` + 12 题 `human_evaluation`），标记为 `manual_check`；因此**答案类指标以 394 道可判分题为分母**，过程正确率以全量 418 题计。

### 8.1 单 Judge（GPT-5.6-terra 外部裁判）结果

为防止 Hy3 自评虚高，单 judge 模式默认使用外部 **GPT-5.6-terra** 作为裁判。418 题主实验集（`dataset/problems_merged_full.jsonl`，合并脚本 `scripts/merge_full_dataset.py`）总体结果：

| 指标 | 数值 |
|---|---|
| 总题数 / 可判分题数 | 418 / **394** |
| 最终答案准确率 | 269 / 394 = **68.27%** |
| 过程正确率 | 353 / 418 = **84.45%** |
| 严格过程正确率（答案对且过程对） | 249 / 394 = **63.20%** |
| 结果正确但过程不成立率（CBU） | 20 / 394 = **5.08%** |
| 答案错误但过程被判正确率 | 84 / 394 = **21.32%** |

按验证方式分布：

| 验证方式 | 题数 | 答案正确率 | 过程正确率 |
|---|---|---|---|
| verifiable_answer（有可判答案） | 394 | 68.27% | 84.52% |
| formal_proof（形式化证明题） | 12 | —（全部 manual_check） | 83.33% |
| human_evaluation（需人工判答案） | 12 | —（全部 manual_check） | 83.33% |

注：**21.32% 的“答案错但过程对”** 比例显著高于 CBU 率（5.08%），其中相当部分来自最终答案抽取与等价性判定的严格性（带单位、选项标签、顺序差异等），也包含推导合理但最后一步誊写/计算失误的样本；该队列与 CBU 队列一样，应视为待人工复核的候选集。

### 8.2 多维分层分析（单 judge）

**按难度分层**（各率以该层可判分题数为分母；L1/L2 全部可判）：

| 难度 | 题数（可判） | 答案正确率 | 过程正确率 | 严格过程正确率 | CBU 率 | 答案错但过程对率 |
|---|---|---|---|---|---|---|
| L1 | 98（98） | 93.88% | 95.92% | 91.84% | 2.04% | 4.08% |
| L2 | 108（108） | 71.30% | 83.33% | 63.89% | 7.41% | 19.44% |
| L3 | 101（91） | 50.55% | 86.81% | 46.15% | 4.40% | 40.66% |
| L4 | 111（97） | 55.67% | 72.16% | 49.48% | 6.19% | 22.68% |

**按问题类型**：

| 问题类型 | 题数（可判） | 答案正确率 | 过程正确率 |
|---|---|---|---|
| elementary_math | 225（222） | 79.73% | 89.64% |
| competition_math | 161（152） | 59.21% | 76.97% |
| college_math | 20（20） | 10.00% | 85.00% |
| formal_math | 12（0） | —（全部 manual_check） | 83.33% |

**按数据集分组**：

| 数据集组 | 题数（可判） | 答案正确率 | 过程正确率 | CBU 率 |
|---|---|---|---|---|
| original_158 | 158（149） | 75.17% | 85.91% | 3.36% |
| ext_228 | 228（213） | 65.73% | 83.57% | 6.10% |
| perturbation_32 | 32（32） | 53.12% | 84.38% | 6.25% |

**按污染风险**：

| 污染风险 | 题数（可判） | 答案正确率 | 过程正确率 |
|---|---|---|---|
| high | 182（182） | 82.42% | 88.46% |
| medium | 121（109） | 47.71% | 80.73% |
| low | 115（103） | 65.05% | 81.55% |

注：medium 组答案正确率最低并不直接意味着“污染少反而更差”——该组 121 题全部为 L3/L4 难题（L3 74 题、L4 47 题），难度构成是主因；high 组以 L1/L2 为主（142/182），其 82.42% 的高答案正确率与污染/记忆风险叠加，需结合扰动簇对照实验（8.5 节）解读。

### 8.3 错误类型分布（单 judge）

| 错误类型 | 出现次数 |
|---|---|
| 无错误 | 353 |
| 跳步推导 | 20 |
| 计算错误 | 14 |
| 其他/无法归类 | 9 |
| 概念理解错误 | 8 |
| 条件遗漏 | 7 |
| 定理/公式误用 | 4 |
| 题意误读 | 3 |
| 循环论证 | 0 |

418 题全量结果中，依赖图验证共标记 **1 道** 结构性问题：L3-035 步骤 4 引用未来步骤 5（前向引用，依赖必须指向更早步骤）；本轮未检出自环/循环依赖。“跳步推导”仍是占比最高的真实错误类型（20 次），与 158 题子集上的观察一致。

### 8.4 结果正确但过程不成立的典型案例

418 题全量结果中，外部 GPT 裁判共标记 **20 道 CBU**（`answer_correct=true` 且 `process_correct=false`，占可判分题 5.08%），全部清单及简述如下（数据直接来自 `results/evaluation_merged_full_enriched.json`）：

| 题号 | 数据集组 | 首错步 | 错误类型 | 简述 |
|---|---|---|---|---|
| L1-022 | original_158 | 2 | 计算错误 | 中间换算等式不成立（4/5=0.8 与 500×4/5=400 混用），被 sympy 检出 |
| L2-005 | original_158 | 5 | 条件遗漏 | 仅验证 x=13 是一个解，未排除其他候选因数，未证明正整数解唯一性 |
| L2-015 | original_158 | 6 | 跳步推导 | 仅凭两个单点的导数符号断定整区间单调性，缺少连续性/符号不变论证 |
| EXT-L1-036 | ext_228 | 5 | 题意误读 | 将题目“十分钟后”误写为“三十分钟后”（不影响数值但与题意不符） |
| EXT-L2-009 | ext_228 | 3 | 计算错误 | 等式不成立：1 ≠ 5+8/1=13 |
| EXT-L2-014 | ext_228 | 5 | 条件遗漏 | 由 P(Y>1600)=0.05 只能推出充分条件而非必要条件，误断 x≥18 为最少 |
| EXT-L2-043 | ext_228 | 7 | 计算错误 | k≥1 时不等式区间实际为空（区间非空需 k≤0），非空判断错误 |
| EXT-L2-044 | ext_228 | 4 | 其他/无法归类 | 凭空假定“乙知道自己成绩”，与题目设定矛盾，推理依据不成立 |
| EXT-L2-065 | ext_228 | 7 | 计算错误 | 等式不成立：f(0)=0 与 (0+2)/(1+1)=1 混用 |
| PERT-L2-008-add | perturbation_32 | 8 | 计算错误 | 质因数分解数值错误：2^6·3^2·11^3=766656 ≠ 876096 |
| L3-013 | original_158 | 3 | 计算错误 | 等式不成立：f(3)/f(6)=1/2 误写为 1/3 |
| L3-035 | original_158 | 4 | 跳步推导 | 步骤 4 引用未来步骤 5，被依赖图检出前向引用 |
| EXT-L3-009 | ext_228 | 6 | 条件遗漏 | 递推遗漏 m=1 边界情形（f(0,2)=2 而非 3），归纳结论不成立 |
| EXT-L4-030 | ext_228 | 2 | 概念理解错误 | 误将填入 9 个不同数字的块 A 称为“3×3 Latin 方阵”，概念不成立 |
| EXT-L4-031 | ext_228 | 5 | 跳步推导 | 负根 t₋≥-1 的区间条件未严格论证，不等式方向无法排除 t₋<-1 |
| EXT-L4-038 | ext_228 | 4 | 跳步推导 | 由非凸性直接排除 x_C=14 分支缺乏严格论证，分支选择无依据 |
| EXT-L4-043 | ext_228 | 5 | 概念理解错误 | “相切的必要条件是二次方程有重根且重根 u₀≥0”断言错误（t=0 情形遗漏） |
| EXT-L4-045 | ext_228 | 10 | 计算错误 | 等式不成立：988-975+1001=1014 与 1/2 混用 |
| EXT-L4-057 | ext_228 | 6 | 概念理解错误 | 将两两乘积和误当作仅循环相邻边乘积和，漏掉 5 个非相邻指标对 |
| PERT-L3-044-surface | perturbation_32 | 2 | 计算错误 | 等式不成立：Q_k(0)=0 与 1 混用 |

需要说明的是，这些案例反映的是**评估器当前的判定结果**，并不等同于“模型推理一定不可接受”。7.5 节的独立审计（基于 158 题子集）表明，裁判标记的 CBU 中约半数经人工复核为评估器过度严格（false_positive），真正“蒙对/巧合”的致命案例只占少数。因此，单 judge 标记的这 20 道 CBU 应被视为**待人工复核的候选集**，而非盖棺定论。

### 8.5 模型能力边界与临界点

- **L1 → L2 是第一道断崖**：答案正确率从 93.88% 降至 71.30%（-22.6 个百分点），严格过程正确率从 91.84% 降至 63.89%（-28.0 个百分点）。题目一旦超出小学/初中应用题范围，约三成题目无法同时通过答案与过程两关。
- **L2 → L3 是第二道断崖**：答案正确率从 71.30% 降至 50.55%（-20.8 个百分点），严格过程正确率从 63.89% 降至 46.15%（-17.7 个百分点），进入高中竞赛级综合推理后可靠性进一步腰斩。
- **严格过程正确率在 L3/L4 仅 46%~49%**：L3 为 46.15%、L4 为 49.48%。注意 L3 的过程正确率（86.81%）反而高于 L2（83.33%），这并非能力回升——L3 有高达 40.66% 的题“答案错但过程被判对”（含大量答案格式/等价性争议），单独看过程正确率会高估难题上的真实表现；**严格过程正确率才是随难度单调下降的真实能力指标**（91.84% → 63.89% → 46.15% → 49.48%）。
- **college_math 是极端短板**：20 题答案正确率仅 10.00%，但过程正确率 85.00%，说明该组判分高度受答案等价性/最终化简影响，需人工复核区分“推导对但答案形式不符”与“真实错误”。
- **GSM-Plus 20 簇扰动对照**（每簇 3 题：原题 / 数值替换 / 干扰插入）：

  | 变体 | 答案正确率 | 过程正确率 |
  |---|---|---|
  | 原题 | 100.00%（20/20） | 100.00%（20/20） |
  | 数值替换 | 80.00%（16/20） | 95.00%（19/20） |
  | 干扰插入 | 95.00%（19/20） | 90.00%（18/20） |

  原题全部答对，但**仅替换数值就使答案正确率下降 20 个百分点**，而插入无关条件只下降 5 个百分点——模型对“数值改变”远比对“文本干扰”敏感，与“记住原题答案而非稳定执行算术”的记忆假说一致。

  > ⚠️ **诚实声明（gold 噪音）**：对数值替换组 4 道“答错”题逐题验算发现，至少 2 道（EXT-L1-023、EXT-L1-032）的模型答案实际正确、gold 标签可疑，另有 1 道（EXT-L1-020）是中文单位导致 answer_checker 解析失败。因此“100% → 80%”的**方向可靠**（与 L2 多采样“答案稳、路径飘”证据互证，见 8.8 节），但**幅度可能被 gold 噪音高估**，真实下降幅度可能小于 20 个百分点。详细验算过程见 `ANALYSIS_REPORT.md` §3.7。

- **16 原题扰动簇对照**（原题按难度均匀抽样，L1~L4 各 4 题；扰动变体共 32 题即 perturbation_32 组）：

  | 变体 | 答案正确率 | 过程正确率 |
  |---|---|---|
  | 原题 | 56.25%（9/16） | 87.50%（14/16） |
  | surface_rewrite | 56.25%（9/16） | 81.25%（13/16） |
  | add_noise | 50.00%（8/16） | 87.50%（14/16） |

  改写题干表述（surface_rewrite）后答案正确率持平、过程正确率下降 6.25 个百分点；加入噪声条件（add_noise）后答案正确率再降 6.25 个百分点。扰动带来的退化方向与 158 题子集上的观察一致，但幅度更温和。
- **形式化证明题（formal_proof / FrontierMath）**：12 题全部 manual_check，无公开答案可判分；其过程正确率 83.33% 仅为裁判语义判断，建议按 7.5 节规范继续人工抽检复核。

### 8.6 回代验证发现（基于 158 题子集）

> 注：本节数据来自 158 题子集（original_158）的 6k 合并评测（`results/evaluation_merged_depgraph_6k_gpt.json`），尚未在 418 题全量上重跑；418 题全量主结果见 8.1–8.5 节。

`back_substitution.py` 把最终答案代回原题约束做独立符号/数值验证。6k 合并结果中：

- 共 **72 / 158（45.57%）** 题触发回代验证；
- 其中 **16 题** 被识别为可自动回代的方程/不等式/优化题型；
- **0 题** 回代验证直接通过，主因包括：
  - 中文应用题无法提取可解析的数学约束（20 题标记为 `unknown`）；
  - 模型输出答案含单位/选项标签，变量赋值解析失败；
  - 约束提取时 LaTeX/文本解析误差（如把 "Let x^8 + 98x^4 + 1 = p(x)q(x)" 误解析为含无关符号的等式）。

回代验证器目前更多作为 **CBU 与 answer_checker 等价性争议的辅助证据**，在纯文字应用题集上的自动约束提取成功率仍有限，后续需结合更鲁棒的题目语义解析。

### 8.7 多采样一致性分析（基于 158 题子集）

> 注：本节实验在 158 题子集（original_158）的分层抽样上进行，尚未扩展到 418 题全量；418 题全量主结果见 8.1–8.5 节。

为探测记忆/背诵与推理不稳定性，对代表性子集（每难度层 5 题，共 20 题）以 `temperature=0.8`、`top_p=0.95` 独立采样 3 次，分析答案一致性与过程一致性。结果见 `results/consistency_analysis_subset_small.json` 与 `results/consistency_analysis_subset_small_report.md`。

**整体指标**：

| 指标 | 数值 |
|---|---|
| 自一致率（多数答案正确率） | **65.00%** |
| 答案一致率 | **75.00%** |
| 过程一致率 | **25.00%** |
| 答案一致但过程不一致率 | **50.00%** |
| 条件过程一致率 | **38.46%** |

**按难度层指标**：

| 难度 | 题数 | 自一致率 | 答案一致率 | 过程一致率 | 答案一致但过程不一致率 |
|---|---|---|---|---|---|
| L1 | 5 | 80.00% | 100.00% | 80.00% | 20.00% |
| L2 | 5 | 80.00% | 100.00% | 20.00% | 80.00% |
| L3 | 5 | 60.00% | 60.00% | 0.00% | 60.00% |
| L4 | 5 | 40.00% | 40.00% | 0.00% | 40.00% |

**关键发现**：

1. **L1 推理最稳定**：答案一致率 100%、过程一致率 80%，说明简单应用题上 Hy3 能通过稳定路径得到正确答案。
2. **L2 出现“答案稳定但路径漂移”**：答案一致率仍达 100%，自一致率 80%，但过程一致率骤降至 **20%**——3 次采样中多数答案正确，但推理路径（步骤数、依赖图结构、公式集合）高度不一致。这与“背过答案/模板，但推理过程是现场拼凑”的记忆型表现高度吻合。
3. **L3/L4 答案与路径双重不稳定**：自一致率分别降至 60% 与 40%，过程一致率为 0%，说明难题上模型不仅答案不确定，推理路径也极不稳定。
4. **50% 的样本存在“答案一致但过程不一致”**：即使在 3 次采样中答案相同，仍有半数题目的推理路径显著不同，提示单次评测中的“答案对 + 过程对”样本可能隐藏路径多样性，CBU 检测需要结合多采样证据才更可靠。

该实验样本量仅 20 题，后续可扩展到 100 题以上以获得更稳定的自一致率估计，但它已为“记忆 vs 推理”的区分提供了可量化的探测手段。

为了改善错误类型分布过于集中于“跳步推导”的现象，对 `evaluator/llm_judge.py` 的 system prompt 增加了每类错误的 few-shot 判例，明确要求裁判在落为“跳步推导”之前先排除题意误读、概念理解错误、计算错误、条件遗漏、定理/公式误用、幻觉/无中生有、循环论证、单位/格式不符等具体类型。

校准后的单 judge 错误类型分布仍显示“跳步推导”占绝对多数（52 次），但外部 GPT 裁判已能识别出条件遗漏、概念理解错误等更多类型。原因有两点：

1. **Hy3 在 L3/L4 难题上的真实失败模式确实以“缺少可验证中间环节”为主**，即步骤之间的逻辑 gap 最突出；
2. **当前解题输出未显式标注步骤依赖关系**，裁判只能凭语感判断循环论证或条件遗漏，难以形成结构性证据。依赖图显式分步（`depends_on`）可将“跳步”转化为依赖链断裂检测，将“循环论证”转化为有向图环检测，从而把这两类错误从语义判断升级为算法判定。这是后续计划中的首要改进方向。

---

## 8.8 L2 层“答案稳路径飘”与记忆 / 模板探测专题分析（基于 158 题子集）

> 注：本节专题实验基于 158 题子集（original_158）中的 35 道 L2 题，尚未在 418 题全量上重跑；418 题全量主结果见 8.1–8.5 节。

### 8.8.1 现象与动机

在多采样一致性实验中，L2 难度层出现了一种值得警惕的模式：**答案高度稳定，但推理路径显著漂移**。对全部 35 道 L2 题以 `temperature=0.8` 采样 3 次后得到：

| 指标 | L2（35 题 × 3 样本） |
|---|---|
| 答案一致率 | **100.00%** |
| 自一致率（多数答案正确率） | **91.43%** |
| 过程一致率 | **34.29%** |
| 答案一致但过程不一致率 | **65.71%** |

这意味着：在 L2 这一“初中级竞赛/应用题”层，Hy3 几乎总能稳定输出正确答案，但其中 **2/3 的题目在多次采样中采用了显著不同的推理路径**。单一评测很难发现这一问题——只看答案会给出 90% 以上的高分，而过程评估揭示出推理路径并不稳定。

### 8.8.2 记忆 / 模板探测指标

为量化“答案稳路径飘”是否源于记忆/模板调用而非稳定推理，设计了三个不调用 LLM 的纯规则指标（实现见 `evaluator/memory_detection_metrics.py`）：

| 指标 | 含义 | L2 结果 |
|---|---|---|
| 解法类型稳定性 | 同题 3 个样本的解法标签是否一致 | 解法漂移率 **28.57%** |
| 依赖图结构稳定性 | 基于 `depends_on` 的边集 Jaccard 相似度 | 图结构漂移率 **37.14%**，平均边 Jaccard **0.6053** |
| 定理 / 公式引用一致性 | 核心定理关键词集合的 Jaccard 相似度 | 定理漂移率 **20.00%**，平均定理 Jaccard **0.8476** |
| 综合记忆漂移标记 | 任一指标触发即标记 | **54.29%**（19 / 35） |

**指标解读**：

- **解法类型漂移率 28.57%** 说明约 1/4 的 L2 题在多次采样中使用了不同的高层面方法（例如换元法 vs 向量模平方公式）。
- **图结构漂移率 37.14%** 更敏感：即使解法名称相同，步骤之间的依赖结构也可能差异很大，反映模型在“同解法的不同模板变体”之间切换。
- **定理引用相对稳定**（平均 Jaccard 0.85），说明一旦模型决定调用某个定理，它通常会在多次采样中重复该定理名称。
- **综合记忆漂移率 54.29%** 意味着超过一半的 L2 题呈现“答案稳定但中间表征不稳定”的模板/记忆指纹。

### 8.8.3 典型案例分析

对 5 道 L2 题做了逐题微观案例分析（详细报告见 `results/l2_case_analysis.md`），归纳出 4 种路径漂移模式：

| 模式 | 代表题号 | 描述 |
|---|---|---|
| **同解法不同验证** | L2-015 | 都用求导法求最小值，但验证 `x=2` 为全局最小值时分别用一阶导符号、二阶导凸性、因式分解三种模板。 |
| **同定理不同表述** | L2-009 | 都用韦达定理求根乘积，但分别用 `-d/a`、首一化 `-c`、先陈述公式再代入三种表述。 |
| **同算法不同粒度** | L2-007 / L2-016 | 算法无分歧，但步骤粗细不同；L2-007 某样本将多步算术压缩为一步。 |
| **答案格式伪错误** | L2-035 | 向量点积计算结果正确（=1），但答案格式分别为 `A`、`(A) 1`、`(A) 1`，后两者被判错。 |

这 5 道题的 `contamination_risk` 全部为 **high**（4 道来自 MATH，1 道来自 AGIEval）。高污染风险与“答案稳定 + 路径在等价模板间漂移”的现象高度吻合：模型很可能在训练语料中见过这些题，记住了答案和多种等价写法，但并未形成统一的推理过程。

### 8.8.4 与污染风险的关联

L2 全部 35 题均来自高污染风险数据集（MATH / AGIEval）。将“记忆/模板漂移”与污染风险联系起来，可以得到以下推论链：

1. **高污染风险 → 答案高稳定性**：模型在预训练/微调中很可能见过原题，因此多次采样都能给出正确答案。
2. **记忆调用 → 路径模板化**：模型不是重新构造解法，而是从记忆中召回“求导法”“韦达定理”“向量模平方公式”等常见模板。
3. **模板变体 → 路径漂移**：同一道题在训练语料中可能存在多种等价讲解，模型在不同采样中随机调用不同变体，导致依赖图和步骤粒度不一致。
4. **过程评估 → 暴露问题**：单纯看答案会严重高估 L2 能力；过程一致性与记忆漂移指标才能揭示“答案对但推理不稳定”的真实情况。

### 8.8.5 结论与建议

1. **L2 是“记忆/模板风险”最突出的难度层**：答案准确率接近 100%，但过程一致率仅 34.29%，综合记忆漂移率 54.29%。
2. **过程一致性是比答案正确率更严格的真实能力指标**：在 L2 上，两者差距高达 65.71 个百分点。
3. **需要区分“路径漂移”与“格式伪错误”**：L2-035 表明答案归一化问题会制造“推理正确但被判错”的噪音，后续应把选择题的“内容正确性”与“格式匹配”分开评分。
4. **后续可做的验证**：对 L2 题目做数字/符号扰动（如 L2-016 的 270 天改为 271 天），若答案稳定性骤降，将进一步支持记忆假说；同时可在低污染风险题集上建立路径漂移基线，量化“记忆依赖度”。

---

### 8.9 418 题三 Judge 全量评估

> 结果文件：`results/evaluation_merged_full_multi_judge.json`。该评估与 8.1 节单裁判结果（`results/evaluation_merged_full_enriched.json`）**共享同一份解答快照**，因此两组答案准确率完全一致，差异只来自过程判定。裁判构成为 Hy3 + GPT-5.6-terra + Gemini-3-flash-preview 三裁判多数投票（配置与聚合策略见 5.5 节）。

#### 8.9.1 总体对比

| 指标 | 单裁判（GPT-5.6-terra） | 三裁判投票（Hy3 + GPT + Gemini） |
|---|---|---|
| 答案准确率 | 269 / 394 = **68.27%** | 269 / 394 = **68.27%**（同一解答快照） |
| 过程正确率 | 353 / 418 = **84.45%** | 292 / 418 = **69.86%** |
| 严格过程正确率（答案对且过程对） | 249 / 394 = **63.20%** | 232 / 394 = **58.88%** |
| 结果正确但过程不成立率（CBU） | 20 / 394 = **5.08%** | 37 / 394 = **9.39%** |
| 答案错误但过程被判正确率 | 84 / 394 = **21.32%** | 52 / 394 = **13.20%** |

#### 8.9.2 难度分层对比

| 难度 | 题数（可判） | 过程正确率（单裁判） | 过程正确率（三裁判） | CBU 率（单裁判） | CBU 率（三裁判） |
|---|---|---|---|---|---|
| L1 | 98（98） | 95.92% | 95.92%（94/98） | 2.04% | 2.04%（2/98） |
| L2 | 108（108） | 83.33% | 88.89%（96/108） | 7.41% | 3.70%（4/108） |
| L3 | 101（91） | 86.81% | 63.37%（64/101） | 4.40% | 10.99%（10/91） |
| L4 | 111（97） | 72.16% | **34.23%**（38/111） | 6.19% | **21.65%**（21/97） |

#### 8.9.3 解读

- **三裁判投票显著更严格**：过程正确率整体下降 14.59 个百分点（84.45% → 69.86%），且严格程度随难度单调放大——L1 两层判定几乎一致（95.92% vs 95.92%），L4 则近乎腰斩（72.16% → **34.23%**），说明单裁判对高难度题的过程审查过于宽松。
- **三裁判下的严格过程正确率恢复单调递减**：91.84%（L1）→ 67.59%（L2）→ 39.56%（L3）→ 34.02%（L4）。单裁判口径下 L4（49.48%）反常高于 L3（46.15%）的非单调假象被消除，L3/L4 的真实过程可靠性比单裁判结论差得多。
- **CBU 率翻倍**（5.08% → 9.39%，20 → 37 道），其中 L4 层 CBU 率高达 **21.65%**：多数投票能抓住单裁判漏掉的"蒙对"样本——难题上只要任一严格裁判发现过程断裂，多数票即不再放行。
- **与 9.3 节 158 题子集结论方向相反，需谨慎解读**：158 题子集上三裁判投票比当次 GPT 单裁判更宽容（CBU 率 12.08% → 5.37%），418 题全量上则显著更严格（5.08% → 9.39%）。注意两组对比的单裁判基线来自不同评估批次，严格度差异很大（过程正确率分别为 56.33% 与 84.45%）；本节的对比与单裁判**共享同一解答快照**，是同口径比较，而 9.3 节的 158 题对比受基线批次波动影响。这提示小样本、跨批次的多裁判结论不宜直接外推。
- 418 题全量的裁判一致性数据在 [9.4 节](#94-裁判间一致性与单裁判稳定性) 中与 158 题旧数据并列呈现。

---

## 9. 多 Judge 交叉复核实验

> 注：本章 9.1–9.3、9.5–9.6 节实验基于 **158 题合并题集**（`results/evaluation_results_merged_multi_judge_fixed.json`）。418 题主实验集上的三 judge 全量评估已完成：结果见 [8.9 节](#89-418-题三-judge-全量评估)（`results/evaluation_merged_full_multi_judge.json`），418 全量裁判一致性数据在 [9.4 节](#94-裁判间一致性与单裁判稳定性) 中与 158 题旧数据并列呈现。

### 9.1 实验设置

引入两个外部裁判：

- **gpt-5.6-terra** @ 外部 OpenAI 兼容 API 网关
- **gemini-3-flash-preview** @ 外部 OpenAI 兼容 API 网关

与本地 Hy3 组成三 judge 投票。

### 9.2 外部模型兼容性修复

- `gpt-5.6-terra` 不支持 `max_tokens`，改为 `max_completion_tokens`。
- `gpt-5.6-terra` 不支持 `temperature` / `top_p`，对 `o1/o3/gpt-5.6` 前缀模型自动 omit。
- `gemini-3-flash-preview` 在同网关下传 `max_tokens` 会返回空 message，已自动跳过该参数。

### 9.3 多 Judge 结果

> 本节数据来自 `results/evaluation_results_merged_multi_judge_fixed.json`。该结果中 149 道为可自动判分题（gradable），另有 9 道 FrontierMath v2 题因无公开 gold 答案而标记为 `manual_check`，故分母在 149 与 158 之间变化；8.1 节的 158 题全量为 `results/evaluation_merged_depgraph_6k_gpt.json`（单 judge、无 `manual_check` 排除）。

| 指标 | 单 judge（GPT-5.6-terra） | 三 judge 投票（Hy3 + GPT + Gemini） |
|---|---|---|
| 最终答案准确率 | 67.79%（101 / 149） | 67.79%（101 / 149） |
| 过程正确率 | 56.33%（89 / 158） | **61.39%**（97 / 158） |
| 严格过程正确率 | 55.70%（83 / 149） | **62.42%**（93 / 149） |
| CBU 率 | **12.08%**（18 / 149） | **5.37%**（8 / 149） |
| 答案错误但过程被判正确 | 3.36%（5 / 149） | 2.68%（4 / 149） |

**关键观察**：

- 单 judge 使用外部 GPT 裁判时，过程正确率（56.33%）**低于**三 judge 投票（61.39%），与此前 Hy3 自审（67.09%）高于三 judge 的情况相反。
- 这说明 **Hy3 自审显著偏宽松**：它倾向于把自己有跳步/条件遗漏的解答判为过程正确；而 GPT 单裁判已经能识别出大量 CBU（12.08%）。
- 三 judge 投票因为纳入了 Hy3 这一宽松裁判，反而比 GPT 单裁判更“宽容”，CBU 率从 12.08% 降至 5.37%。
- 因此，若目标是严格刻画过程严谨性，**使用外部强模型（如 GPT-5.6-terra）单裁判** 比 Hy3 自审 + 多 judge 投票更能暴露真实问题。

### 9.4 裁判间一致性与单裁判稳定性

为验证 LLM-as-judge 的可靠性，在 158 道样本上计算三裁判一致率与配对 Cohen's κ，并在 20 道分层样本上做单 judge 重复稳定性测试（同一批样本用 Hy3 裁判独立评估 3 次）。

**三裁判一致率**（`results/judge_agreement_stats_fixed.json`，基于 158 道合并题集）：

| 指标 | 数值 |
|---|---|
| 三裁判完全一致 | 76 / 158 = **48.10%** |
| 多数同意（≥2 票相同） | 92 / 158 = **58.23%** |
| 过程正确性两两一致率（Hy3 vs GPT） | 82.76%，κ = 0.634 |
| 过程正确性两两一致率（Hy3 vs Gemini） | **98.91%**，κ = **0.852** |
| 过程正确性两两一致率（GPT vs Gemini） | 83.70%，κ = 0.243 |
| 错误类型三裁判全同（至少一方判错样本） | 1 / 85 = **1.18%** |
| 首错步三裁判完全匹配（至少一方判错样本） | 2 / 77 = **2.60%** |
| 首错步 ±1 步内匹配 | 2 / 77 = **2.60%** |

**418 题全量一致性**（基于 `results/evaluation_merged_full_multi_judge.json` 重算，与 158 题旧数据并列）：

418 题中有效三元组 **339 个**（79 题存在某裁判 API/解析失败，其投票未计入一致性统计）。

| 指标 | 158 题子集（旧） | 418 题全量（新） |
|---|---|---|
| 过程正确性三裁判完全一致率 | 76 / 158 = **48.10%** | 271 / 339 = **79.94%** |
| 过程正确性两两一致率 | 82.76% / 98.91% / 83.70% | 84.96% / 90.56% / 84.37% |
| 过程正确性两两 Cohen's κ | 0.634 / 0.852 / 0.243 | 0.551 / 0.598 / 0.529 |
| 错误类型三裁判全同（至少一方判错样本） | 1 / 85 = **1.18%** | 9 / 98 = **9.18%** |
| 首错步三裁判完全匹配（至少一方判错样本） | 2 / 77 = **2.60%** | 11 / 98 = **11.22%** |

**全量 vs 子集解读**：

- **全量规模下裁判一致率反而更高**（79.94% vs 48.10%），说明 158 题子集的小样本一致性估计偏悲观。两个结构性因素抬高了全量一致率：79 个失效三元组高度集中于 L3/L4（72/79），使有效三元组中 L1/L2 占比升至 58.7%（199/339）；而三裁判在易题上普遍一致判对（L1 一致率 93.62%、L2 80.95%，L4 仅 61.43%）。
- **两两 κ 收敛到 0.53–0.60 的中等一致区间**：旧数据中 Hy3–Gemini 高达 0.852 而 GPT–Gemini 仅 0.243 的极端分化在全量上消失，三对裁判的一致性趋于均衡。
- **"错误位置"判定依然远比"是否有错"不稳定**：首错步完全匹配率仅从 2.60% 升至 **11.22%**，错误类型全同率也仅 9.18%——即便在"过程是否有错"上达成多数共识，三裁判对"错在哪一步、属于哪类错误"的判定仍然高度分散；错误类型分布与首错步指标应继续以趋势参考为主。

**单裁判稳定性**（`results/single_judge_stability.json`）：

| 指标 | 数值 |
|---|---|
| 20 样本 × 3 次重复 | `overall_valid` 一致率 **100%** |
| 首错步完全一致 | 100% |
| 错误类型完全一致 | 100% |

**解读**：

- Hy3 与 Gemini 在“过程是否成立”上高度一致（κ = 0.852），而与 GPT 一致性中等（κ = 0.634）。
- GPT 与 Gemini 一致性低（κ = 0.243）主要因为 GPT 对“过程成立”的门槛更宽松， Gemini 更严格，两者在 L3/L4 难题上的分歧最大。
- 单裁判在固定样本、固定 prompt 下具有完美的重复稳定性，说明评估器输出波动主要来自模型自身采样，而非 prompt 解析随机性。
- 三裁判完全一致率仅 48.1%，意味着 LLM-as-judge 在复杂数学过程上存在显著的主观差异；多 judge 投票是抑制个体差异的必要手段。
- 错误类型与首错步的一致率极低（约 1–3%），说明 judge 对“错误原因”和“错误位置”的判定比“是否有错”更不稳定；当前错误类型分布应以趋势参考为主，不宜过度解读单条样本的类型标签。

### 9.5 污染风险分组（多 judge）

按 `contamination_risk` 字段分组的三 judge 投票结果如下，可与 4.5 节单 judge 结果对照：

| 风险等级 | 题数 | 答案正确率 | 过程正确率 | 严格过程正确率 | CBU 率 |
|---|---|---|---|---|---|
| high | 77 | 94.81% | 94.81% | 90.91% | 3.90% |
| medium | 69 | 40.58% | 34.78% | 33.33% | 7.25% |
| low | 12 | 0.00% | 0.00% | 0.00% | 0.00% |

与 GPT 单裁判相比，三 judge 投票因为纳入了更宽松的 Hy3 自审，high 风险组的过程正确率从 84.42% **上升**至 94.81%，CBU 率从 14.29% **下降**至 3.90%；medium 风险组的过程正确率从 33.33% 微升至 34.78%，CBU 率从 10.14% 降至 7.25%。low 风险组（FrontierMath-v2）在三 judge 下仍全部未答对。这一对比再次说明：外部严格裁判比多 judge 投票更能暴露常见数据集上的“答案对但过程不成立”现象，污染/记忆风险可能被过程评估显著放大。

### 9.6 难度分层（多 judge）

| 难度 | 题数 | 答案正确率 | 过程正确率 | 严格过程正确率 | CBU 率 |
|---|---|---|---|---|---|
| L1 | 30 | 96.67% | 100.00% | 96.67% | 0.00% |
| L2 | 35 | 100.00% | 94.29% | 94.29% | 5.71% |
| L3 | 47 | 53.19% | 53.19% | 46.81% | 6.38% |
| L4 | 46 | 26.09% | 19.57% | 19.57% | 6.52% |

---

## 10. 反向验证器实验（Math-Shepherd 思想）

> 方法与实测的完整报告见 [`results/reverse_verifier_report.md`](results/reverse_verifier_report.md)，本章为要点摘录与结论。结果文件：`results/reverse_verification_test.json`。

### 10.1 方法原理

本实验实现 Math-Shepherd（Wang et al. 2023）的**推理时版本**，无需训练奖励模型：一个解题步骤的"好坏"不由裁判主观判断，而由**该步骤之后的推理到达正确答案的经验概率**决定。对一条解答的步骤序列 s₁..sₙ：

1. **前缀取点**：最多取 6 个前缀采样点（首步、末步，中间均匀取点），长解答不必逐步全测；
2. **续写采样**：对每个前缀 (s₁..sᵢ)，把"题目 + 已有步骤（声明为正确步骤，要求信任并继续）"作为上下文，让 Hy3 以 temperature=0.7 续写采样 N 次（默认 4），每次续写提取 `\boxed{}` 最终答案；
3. **打分**：步骤 i 的得分 = N 次续写中到达正确答案的经验比例（hard estimation），答案比对复用 `dataset/answer_checker.py` 的 `check_answer`；
4. **聚合判定**：首错步 = 前缀分从 ≥ 0.5 骤降到 < 0.25 的第一个采样步骤（若首个采样点已 < 0.25，则首错步不晚于该点）；min 前缀分 < 0.25 判过程不成立。

### 10.2 实现

- 核心模块：`evaluator/reverse_verifier.py`；批量 CLI：`scripts/run_reverse_verification.py`；
- 复用 `app/hy3_client.py`（OpenAI 兼容 API）与 `dataset/answer_checker.py`，未改动 `evaluator/`、`app/`、`dataset/` 下任何现有文件；
- 所有题的（前缀 × 采样）任务铺平到一个 `ThreadPoolExecutor` 并发执行，并发度由 `--batch_size` 控制；
- 针对 `answer_checker.extract_boxed_answer` 的正则无法处理嵌套花括号（`\boxed{\frac{1}{2}}` 被截断为 `\frac{1`）的问题，在模块内实现了括号计数的 `_extract_boxed_nested` 作为续写输出提取器（未改动 answer_checker 的既有行为）；
- 模块自检：`python -m evaluator.reverse_verifier`；复现命令见 `results/reverse_verifier_report.md` §5。

### 10.3 15 题实测结果

子集构成：5 条答案正确且过程正确 + 6 条答案判错（其中 L2-022、EXT-L2-005 经人工复核为 checker 误判）+ 3 条 CBU 注入样本（CBU-INJ-001/004/006）。参数：N=4、max_prefixes=6、temperature=0.7、max_tokens=1536、并发 16；共 264 次续写调用，总耗时 112.7s（≈7.5s/题）。对比基线为 GPT-5.6 judge 结论。汇总如下（逐题对比明细见 `results/reverse_verifier_report.md` §3.1）：

| 组别 | n | GPT judge 判对 | 反向验证器判对 |
|---|---|---|---|
| 答案正确且过程正确 | 5 | 5/5 | 5/5 |
| 答案错误且过程确有错误 | 5 | 5/5 | 5/5 |
| checker 误判为错（实际正确/基本正确） | 2 | 1/2 | 0/2 |
| CBU 注入（答案对过程错） | 3 | 2/2（有记录者） | 2/3 |

亮点与问题案例：

- **CBU-INJ-001**（步骤 2 注入"加法=乘法"错误）：反向验证器**精确定位注入步**（min 分 0.00 @步骤 2），与 judge 一致；
- **CBU-INJ-006**（步骤 1 注入 3×2=5，无 judge 记录）：反向验证器独立抓住并精确定位步骤 1；
- **CBU-INJ-004**（步骤 2 用 1/5 误算 25%，步骤 3 打补丁 +4 仍得正解 20）：**反向验证器漏判**——错误在逻辑上自相矛盾但不阻断通往正确答案的路径，续写模型照样从该前缀走到正解，judge 反而能抓住（"方法性失明"，见 10.5）；
- **L2-022 / EXT-L2-005**：仅有的两条"误伤"根源均在 answer_checker 的选择题/gold 格式归一化（gold `(B)144km` vs 预测 `144 km` 空格不匹配；gold 裸字母 `B` vs 预测数值 `-3/2`），而非续写采样本身——反向验证器的判错上限受 checker 精度约束。

整体统计：判过程对 6 / 判错 9，判错的 9 条全部给出首错步定位；平均 min 分 0.383，平均 mean 分 0.578。

### 10.4 与 LLM Judge 的互补性

- **无误伤真正确过程**：5 条正确过程全部高分通过（min ≥ 0.75）；真实错误 5/5 全抓住；
- 反向验证器对"逻辑矛盾但结局不变"的错误系统性失明（CBU-INJ-004），而 LLM judge 对"答案对但过程可疑"的语义审查更强——**两者互补而非替代**；
- 反向验证器的优势是**无需相信裁判、结论可由采样分布复算**，为过程判定提供独立于 LLM 语感的一路客观信号；
- 落地建议：生产中把反向验证器的 min 分作为与 judge 并列的一路信号，低分触发人工/多 judge 复核而非直接判错。

### 10.5 局限分析

工程性局限（可通过修 checker 缓解）：

1. **答案提取/归一化依赖**：反向验证器的分数完全由 `check_answer` 决定，继承其全部盲区——嵌套 boxed 提取 bug（已在模块内绕过，建议后续在 answer_checker 中修复）、选择题 gold 格式压分（**对选择题的判定结论不可直接采信**）、多部分答案只提取到第一部分导致分数恒为 0；
2. **采样方差**：N=4 时单个前缀分的标准误最高达 0.25，EXT-L2-018 的前缀分出现 0.25→1.0→0.0→0.25→0.5 的波动，定位随之漂移；稳定定位需更大 N（8~16）或对相邻前缀做平滑。

方法性局限（本质）：

1. **只惩罚"影响结局"的错误**：若错误步骤逻辑荒谬但不妨碍到达正确答案（CBU-INJ-004），续写模型照样能从该前缀走到正解，反向验证器给出满分；
2. **题意误读无法归位**：若模型从题目本身就一致地误读（EXT-L1-038），从第 1 步前缀续写也会犯同样错误，定位粒度退化为"步骤 1 或更早"；
3. **续写能力依赖**：前缀之后的题目如果太难（L4/研究级），即使前缀完全正确续写成功率也低，正确过程会被低估——缓解办法是只把低分当作"存疑"信号而非判据，或与 judge 投票结合；
4. **定位语义与 judge 不同**：反向验证器定位的是"记录轨迹不可逆偏离正解的位置"。L2-023 中 judge 认为步骤 1 误读题意，但从步骤 1 续写的模型 3/4 能自己读对题并到达正解，只有记录轨迹在步骤 6 落下错误数值——反向验证器定位步骤 6 有其道理，但与人工标注的"首错步"口径不一致，做定位指标时需先统一口径；
5. **对选择题有"猜中率"底噪**：续写乱答也有约 1/选项数 的概率蒙对字母，抬高坏前缀的分数；本次样本中选择题 checker 过严（压分）恰好掩盖了这一点，修复 checker 后该底噪会显现。

---

## 11. 结论与后续计划

### 11.1 主要结论

1. **最终答案判分不足以反映真实能力**：Hy3 在 **418 题主实验集**上最终答案准确率 **68.27%**（269 / 394 可判分题），过程正确率 **84.45%**（353 / 418），严格过程正确率（答案对且过程对）仅 **63.20%**（249 / 394）。另有 **20 道（5.08%）** 答对题目被判定为“结果正确但过程不成立”（CBU），**84 道（21.32%）** 答案错误但过程被判正确（多为答案格式/等价性问题），说明最终答案过关并不等价于推理成立。
2. **过程错误随难度快速增加**：
   - **答案准确率断崖**：**L1 → L2** 从 93.88% 降至 71.30%（-22.6pp），**L2 → L3** 从 71.30% 降至 50.55%（-20.8pp）。
   - **严格过程正确率断崖**：**L1 → L2** 从 91.84% 降至 63.89%（-28.0pp），**L2 → L3** 从 63.89% 降至 46.15%（-17.7pp）。
   - **L3/L4 严格过程正确率不足 50%**：Hy3 在高中竞赛级及以上难题上，即使偶尔答对，也很少能同时给出严谨推导。
3. **答案错但过程对的比例随难度上升**：L1 仅 4.08%，L2 升至 19.44%，L3 高达 **40.66%**。这说明在难题上，模型推理过程往往局部合理，但最终答案因计算/格式/收尾步骤出错而被判错；也提示 answer_checker 的等价性归一化仍有改进空间。
4. **结构性验证层有效补充了语义裁判**：
   - 依赖图验证在 418 题中检出 **1 道** 前向引用（L3-035），无自环，说明 Hy3 在当前 prompt 下较少出现显式循环论证；
   - 回代验证器在 418 题上触发，主要作为 CBU 与 answer_checker 等价性争议的辅助证据；
   - 多采样一致性分析用于探测记忆/背诵与推理不稳定性（结果见 8.7 节，基于 158 题子集）。
5. **评估器可靠性经多维度验证**（基于 158 题子集及合成注入样本）：
   - 合成注入错误：精确步定位准确率 **86.96%**（20 / 23），±1 步命中率 **95.65%**（22 / 23），误报率 **0%**（0 / 5）。
   - 真实答错题：按规范人工重审 30 道，评估器与人工复核在“过程是否成立”上一致率 **96.67%**（29 / 30）；首错步精确命中率 **51.85%**（14 / 27），±1 步命中率 **55.56%**（15 / 27）。
   - 答案正确样本：规范批次 C 抽检 20 道，漏判率 **5%**（1 / 20）；扩展抽检 35 道，漏判率 **2.86%**（1 / 35）；合并 55 道约 **3.64%**（2 / 55）。
   - CBU 注入样本：9 道合成 CBU 检出 7 道，检出率 **77.78%**；计算/定理/概念/幻觉类 100% 检出，跳步/循环论证类仍依赖 judge 语义判断。
   - 单 judge 重复稳定性 **100%**；Hy3 与 Gemini 一致性 κ = **0.852**。
6. **多 judge 是抑制个体差异的必要手段**：三裁判在“过程是否成立”上完全一致率仅 48.1%，多数同意率 58.2%；GPT 与 Gemini 对过程门槛差异显著（κ = 0.243）。多 judge 投票能在不引入系统偏误的前提下提高评估严格度。
7. **污染/记忆风险可被过程评估间接度量**：
   - high 风险组（GSM8K/MATH 等常见数据集）答案正确率 **82.42%** 显著高于 medium 组（47.71%），但 medium 组过程正确率仍保持 **80.73%**，说明过程评估能有效区分记忆与推理。
   - **GSM-Plus 20 簇对照实验**：原题（GSM8K）答案正确率 **100%**，数值替换后降至 **80%**，干扰插入后为 **95%**——仅替换数字就导致 20% 的准确率下降，这是**记忆/背诵**的典型指纹（注意：数值替换组有 2~3 道存在 gold 标签噪音，方向可靠但幅度可能被高估，详见 8.5 节诚实声明）。
   - 16 原题扰动簇：原题 56.25%，surface_rewrite 56.25%，add_noise 50.00%，表面改写和干扰插入都未造成大幅波动，说明这些题更多依赖真实推理。
8. **L2 层存在显著的“答案稳路径飘”记忆/模板指纹**：对 35 道 L2 题采样 3 次，答案一致率 **100%**、自一致率 **91.43%**，但过程一致率仅 **34.29%**，综合记忆漂移率 **54.29%**。路径漂移主要表现为“同解法不同验证”“同定理不同表述”“同算法不同粒度”以及“答案格式伪错误”，与 MATH/AGIEval 高污染风险高度吻合，是记忆/模板调用的典型特征。
9. **三裁判全量投票比单裁判严格得多，且难度越高差异越大**：418 题三裁判投票的过程正确率 **69.86%**（单裁判 84.45%），L4 层从 72.16% 近乎腰斩至 **34.23%**；CBU 率翻倍至 **9.39%**（L4 高达 21.65%）。三裁判口径下严格过程正确率恢复单调递减（91.84% → 67.59% → 39.56% → 34.02%），消除了单裁判在 L4 的宽松假象。全量规模下三裁判完全一致率反而升至 **79.94%**（158 题子集为 48.10%），说明小样本一致性估计偏悲观；但首错步完全匹配率仅 **11.22%**，"错误位置"判定仍远比"是否有错"不稳定（详见 8.9、9.4 节）。
10. **反向验证器（Math-Shepherd 推理时版本）验证可行，并与 LLM judge 互补**：15 题实测中正确过程零误伤（5/5）、真实错误全抓住（5/5）、结局被错误改变的 CBU 注入全部精确定位（CBU-INJ-001 @步骤 2、CBU-INJ-006 @步骤 1）；其判错结论可由采样分布复算、不依赖裁判主观判断，但对"逻辑矛盾但结局不变"的错误系统性失明（CBU-INJ-004），且判错上限受 answer_checker 归一化精度约束（详见第 10 章）。

### 11.2 已完成增强与未来工作

**已在本阶段完成**：

- **依赖图显式分步**：`evaluator/dependency_graph.py` 已上线，将“跳步”检测转化为依赖链断裂判定，将“循环论证”检测转化为有向图环检测，并在 6k 合并结果中实际检出 2 例循环依赖。
- **回代验证器**：`evaluator/back_substitution.py` 已集成到 `process_evaluator.py`，作为 CBU 与答案等价性争议的辅助证据。
- **多采样一致性分析**：`evaluator/consistency_analyzer.py` + `app/multi_sample_generator.py` 已实现，用于探测记忆/背诵与推理路径稳定性。
- **418 题主实验集三 Judge 全量评估**：已完成 386 题扩展集 + 32 题扰动变体（合计 418 题）的全量生成与 Hy3 + GPT-5.6-terra + Gemini 三裁判投票评估，结果见 8.9 节与 `results/evaluation_merged_full_multi_judge.json`。
- **反向验证器与细粒度步骤奖励（推理时版本）**：`evaluator/reverse_verifier.py` + `scripts/run_reverse_verification.py` 已实现 Math-Shepherd 思想的推理时版本并完成 15 题实测（见第 10 章）；**训练版步骤奖励模型（PRM）留作未来工作**。

**未来工作**：

- **错误类型再校准**：在依赖图基础上补充每类错误的结构化判据（如条件遗漏 = 后续结论引用了未声明的前提），并引入更多 few-shot 判例，减少“跳步”作为兜底类型的比例。
- **回代验证器鲁棒化**：改进中文应用题的约束提取与答案变量赋值解析，提升自动回代成功率。
- **训练版细粒度步骤奖励模型（PRM）**：反向验证器的推理时版本已完成（见第 10 章）；未来工作为利用其产出的步骤级奖励数据训练细粒度 PRM，替代采样式打分以降低调用成本，并与 LLM judge 投票融合。前置条件是先修复 answer_checker 的选择题/嵌套 boxed/多部分答案归一化问题，并将采样数 N 提到 8~16 后在更大规模题集上运行。
- **扩大扰动变体对照实验**：引入“换数字”变体，并将样本量扩展到 100 对以上，获得更稳定的“记忆依赖度”估计。
- **L2 记忆/模板探测深化**：对 L2 题目做数字/符号扰动，结合多采样一致性指标与记忆漂移指标，量化“记忆依赖度”与“真实推理度”的边界。
- **置信度校准与分层抽检**：对裁判分歧大、规则层与语义层冲突的样本输出置信度分数，优先进行人工复核，形成“自动评估 → 置信度排序 → 人工抽检”的闭环。

---

## 12. 交付物清单

| 交付物 | 路径/说明 |
|---|---|
| 应用源码 | `app/`：Hy3 客户端、提示词、解析器、批量生成、多采样生成 |
| 过程评估模块 | `evaluator/`：八层评估器（规则/符号/语义/截断/依赖图/回代/一致性/多 judge）、错误分类 |
| README | `README.md`：项目介绍、环境配置、快速开始、结果 |
| 完整项目报告 | `PROJECT_REPORT.md`（本文件） |
| 踩坑记录与经验总结 | `PROJECT_LESSONS.md` |
| 环境配置样例 | `.env.example` |
| 分层题集 | `dataset/problems.jsonl`、`dataset/frontiermath_v2_converted.jsonl`、`dataset/problems_merged.jsonl`、`dataset/problems_merged_v2.jsonl`、`dataset/problems_consistency_subset.jsonl` |
| 数据集扩展脚本 | `scripts/expand_dataset_v2.py` |
| 扩展评测集报告 | `results/dataset_expansion_report.md` |
| 答案校验脚本 | `dataset/answer_checker.py` |
| 过程评估脚本 | `evaluator/evaluate.py`、`evaluator/validate_evaluator.py`、`scripts/rebuild_evaluation.py` |
| 6k token + 依赖图 + GPT 裁判评测结果 | `results/solutions_merged_depgraph_6k.jsonl`、`results/evaluation_merged_depgraph_6k_gpt.json` |
| 单 judge 评测结果（GPT-5.6-terra 外部裁判） | `results/evaluation_results_merged_gpt_judge.json`、`results/metrics_report_merged_gpt_judge.md`、`results/final_report_gpt_judge.md` |
| 多 judge 评测结果 | `results/evaluation_results_merged_multi_judge_fixed.json`、`results/metrics_report_merged_multi_judge_final.md`、`results/final_report_multi_judge_final.md` |
| Hy3 自审对照结果（旧版单 judge） | `results/evaluation_results_merged_final.json`、`results/metrics_report_merged_final.md`、`results/final_report_final.md` |
| 多采样一致性分析 | `results/consistency_analysis_subset.json`、`results/consistency_analysis_subset_report.md`、`results/consistency_analysis_l2_all.json` |
| L2 记忆 / 模板探测 | `evaluator/memory_detection_metrics.py`、`results/l2_memory_metrics.json`、`results/l2_memory_metrics_report.md`、`results/l2_case_analysis.md`、`results/l2_consistency_summary.json` |
| 有效性验证数据 | `results/validation_results_v2.json`、`results/judge_agreement_stats.json`、`results/single_judge_stability.json`、`results/evaluation_cbu_injection.json`、`validation/validation_summary.md`、`validation/frontiermath_spot_check.md` |
| 标准化审查脚本 | `scripts/gpt_manual_audit.py`：按《过程评估人工标注规范》执行人工复核 |
| 人工复核标注文件 | `audit_outputs/gpt_annotation_batch_A.jsonl`、`audit_outputs/gpt_annotation_batch_B.jsonl`、`audit_outputs/gpt_annotation_batch_C.jsonl` |
| 一键运行脚本 | `scripts/run_full_pipeline.sh`、`scripts/run_merged_pipeline.sh` |
| 污染风险标注脚本 | `dataset/assign_contamination_risk.py`：按题源自动标注 `contamination_risk` |
| 扰动变体生成脚本 | `dataset/perturb_problems.py`：对高风险题目生成情境/条件扰动变体 |
| 扰动变体题集 | `dataset/problems_perturbed.jsonl`：扰动后的题目与答案 |
| 扰动索引 | `dataset/perturbation_index.json`：原题 ID 到扰动变体 ID 的映射 |
| 418 题三 judge 全量评估结果 | `results/evaluation_merged_full_multi_judge.json` |
| 反向验证器（Math-Shepherd 推理时版本） | `evaluator/reverse_verifier.py`、`scripts/run_reverse_verification.py`、`results/reverse_verifier_report.md`、`results/reverse_verification_test.json` |

---

*报告生成时间：2026-08-27*
