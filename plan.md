# 数学推理过程评估：文献调研与项目规划

> 个人研究笔记：记录动手之前的文献调研、方法选型思考与目标制定。
> 选题方向：基于混元 Hy3 的数学解题 + 推理过程评估（竞赛题 / 高考题 / 应用题）。

---

## 1. 调研动机：为什么不满足于"答案正确率"

主流数学 benchmark（GSM8K、MATH）只比对最终答案。我关心的核心问题是：**答案对了，模型真的会这道题吗？**

两个直觉驱动了这个调研：

1. 选择题有 25% 蒙对概率，开放题也可能"误用定理却歪打正着"——只看答案会系统性高估能力；
2. 大模型训练语料很可能包含了这些公开数据集，高分里有多少是记忆、多少是推理，单看答案无法区分。

带着这两个问题去查文献，发现过程评估（process-level evaluation）已经有一条清晰的研究线，下面是调研记录。

---

## 2. 文献调研

### 2.1 过程评估范式

**ProcessBench（Qwen 团队，arXiv 2412.06559）** —— 本调研的起点：

- 任务形式化得很干净：给定题目 + 分步解答 S = {s₀,...,s_{n-1}}，输出**最早**错误步索引，全对输出 -1。首个错误之后的步骤即使局部合法也不算对——只评第一处错；
- 关键发现：在 OlympiadBench 上，答案正确的解答中 **32.2%** 存在过程错误；Omni-MATH 上高达 **51.8%**。这直接证实了"只看答案会高估模型能力"；
- 错误类型骨架：计算/代数错误、逻辑错误、概念错误、完整性错误——我的分类体系在此之上扩充；
- 指标设计：错误样本与正确样本分别算准确率再取调和平均，同时惩罚误报与漏报。

**Let's Verify Step by Step（OpenAI，2023，PRM800K）**：

- 过程奖励模型（PRM）按步打分显著优于只看结果的结果奖励模型（ORM）；
- 启发：步骤级监督信号是有价值的；但训练 PRM 成本高，本项目考虑推理时的替代方案。

**Math-Shepherd（2023）**：

- 用蒙特卡洛思路给中间步打标签：从某步出发多次续写，能到达正确答案的比例即为该步的"好坏"分数；
- 启发：**不需要训练**也能用这个思想做推理时验证器——从 Step k 结论独立重采样 N 次，"无法到达正确答案"就是高置信的"此步可疑"信号。这成为本项目反向验证器的设计来源。

### 2.2 题集构造与扰动方法

| 论文 | 核心思想 | 对本项目的启发 |
|---|---|---|
| GSM-Symbolic（Apple, 2024） | 模板化符号扰动；插入无关子句（GSM-NoOp）后强模型性能暴跌 | 无关条件干扰是测"真理解 vs 模板匹配"的低成本手段 |
| GSM-Plus（2024） | 8 种扰动算子（换数字、换情境、加干扰等） | 现成的扰动算子库，每种算子对应一类潜在失败模式 |
| MetaMath（ICLR 2024） | 正向改写 + 反向推理（FOBAR） | 反向变体可暴露"套路记忆 vs 真理解" |
| MathScale（微软） | 概念图抽取 → 随机游走生成新题 | 概念组合深度可作为难度分层的量化依据 |
| Orca-Math（微软） | Agent-Instruct 迭代加难 + 生成-过滤闭环 | 强模型生成题必须配"生成-复核"闭环才能入库 |

### 2.3 调研小结

- 过程评估的必要性已被 ProcessBench 定量证实，但**针对国产开源模型的系统性过程画像还很少**——这是本项目的位置；
- 纯 LLM-as-judge 波动大，需要规则/符号验证做确定性兜底；
- 扰动变体是区分记忆与推理的关键工具。

---

## 3. 研究问题与目标

**研究问题：**

- RQ1：Hy3 在数学推理上的"答案正确率"与"过程正确率"差距有多大？差距随难度如何变化？
- RQ2：Hy3 的失败模式分布是什么样的（计算错、跳步、条件遗漏、循环论证……）？是否存在能力临界点？
- RQ3：Hy3 的高分里有多大比例可由记忆/污染解释？（用扰动对照间接度量）

**方法学目标（评估器自身的可信度）：**

- 设计分层混合的过程评估器，并用标准答案验证其可靠性：
  - 定位准确率：答案错误的样本上，能否定位到真实首错步；
  - 误报率：答案正确的样本上，被判"过程有问题"的样本经人工复核后真实问题占比；
  - 「答案对过程错」样本的检出能力。

**工程目标：** 可运行的解题+批改应用、分层题集（含标准答案与自动校验）、完整结果与案例分析报告、demo 演示。

---

## 4. 初步方案：分层题集构造

### 4.1 规模与分层

初步规划 120~150 题、四层结构（实际执行时扩展到了 418 题）：

| 层级 | 难度定位 | 计划数量 | 主要题型 | 主要用途 |
|---|---|---|---|---|
| L1 基础 | 小学/初中应用题 | 30 | 四则应用题、比例、一元一次方程 | 测评估器**误报率**（正样本池） |
| L2 中等 | 中考/高一 | 40 | 函数、几何计算、数列、概率统计 | 过程问题开始暴露 |
| L3 较难 | 高考压轴/竞赛入门 | 40 | 导数综合、圆锥曲线、组合计数 | 「答案对过程错」高发区，重点观察 |
| L4 高难度 | 竞赛级（AMC/AIME 档） | 30 | 数论、组合、多步综合 | 刻画能力临界点 |

题型配比考虑：开放式计算/证明题 ~70% + 选择题 ~30%（选择题是"蒙对"的天然高发题型）。

### 4.2 数据源设想

- **L1**：GSM8K、Math23K/Ape210K 抽样（自带分步解答）；
- **L2**：MATH Level 2-3、高考真题/AGIEval（高考评分细则天然按步骤给分，可作分步标注参考）；
- **L3**：MATH Level 4-5、Omni-MATH/OlympiadBench 中低档、AMC10/12（AoPS 附社区解答）；
- **L4**：AIME 真题（答案为 0-999 整数，自动校验极方便）、Omni-MATH 高档、IMO/CMO 少量（能力天花板探测）；
- **补充构造**：改造反例（改变条件使套路失效、加冗余干扰条件、设置巧合数值诱捕"蒙对"）、强模型生成变体（标准答案必须经 SymPy 或人工复核）、人工注入样本（在正确解答的 Step k 注入特定错误但保持答案不变，记录注入位置作为定位验证的 ground truth）。

### 4.3 入库格式草稿

```json
{
  "id": "L3-017",
  "source": "AMC12-2019-B-18",
  "tier": "L3",
  "domain": "组合计数",
  "type": "open",
  "problem": "...",
  "answer": "42",
  "answer_check": "numeric",
  "reference_steps": ["..."],
  "is_adversarial": false,
  "injected_flaw": null,
  "cluster_id": "gsm8k-xxx-v2"
}
```

另设**题簇（cluster）**结构：种子题 + 2~3 种扰动算子变体归一簇，支持过程一致性分析。

---

## 5. 初步方案：评估器架构设想

### 5.1 多通道交叉验证 + 仲裁

针对单一 LLM-as-judge 波动大的问题，设想三通道并行：

```
解答 Step k
 ├─ 通道 A：符号/规则校验（SymPy 重算等式变换、数值运算）
 │          → 计算类错误给出确定性结论，零幻觉
 ├─ 通道 B：LLM 分步裁判（输入：题目 + 参考解 + 前序步骤判定历史）
 │          → 覆盖概念、逻辑、条件类语义错误
 └─ 通道 C：反向验证器（Math-Shepherd 思想）
            → 从 Step k 结论独立重采样 N 次，看能否到达正确答案
仲裁层：三通道投票；通道冲突时标记低置信，进入人工复核队列
```

计划做对照实验：混合架构 vs 纯 LLM judge，在同一验证集上比较误报率。

### 5.2 依赖图显式化的分步格式

要求解答每步声明依赖的前序步骤（`Step 4: 由 [Step 1, Step 3] 得 ...`）：

- 错误定位从"线性第几步"升级为"依赖图节点"；
- **跳步** → 依赖链断裂的确定性检测；**循环论证** → 图上环检测；
- 成本仅为 prompt 设计，让这两类错误不依赖裁判语感。

### 5.3 错误区间与传播分析

每个错误的输出不止一个步号，而是一个结构：

```json
{
  "first_error_step": 3,
  "error_type": "条件遗漏",
  "propagation": "fatal",
  "recoverable_at": null
}
```

- `propagation`：fatal（后续全盘皆错）/ benign（笔误但后续自我修正）；
- 可以回答一个有趣的问题：模型犯错后是"一路错到底"还是"有机会自愈"；
- 「答案对过程错」也可细分：benign × 答案对 = 侥幸过关；fatal × 答案对 = 纯蒙对。

### 5.4 错误分类体系草稿（在 ProcessBench 四分法上扩充）

题意误读（含被无关条件干扰）、概念/定理误用、计算/代数错误、条件遗漏、跳步推导、循环论证、幻觉（捏造题目未给的条件）、格式不符。

### 5.5 备选扩展（视时间而定）

- 自适应对抗采样：统计高频错误类型 → 定向生成第二批题 → 迭代，观察定向题集正确率衰减曲线；
- 题簇过程一致性指标：同簇内过程正确率的方差，"答案没变但过程崩了"说明靠运气答对；
- 裁判置信度校准：按置信度分层人工抽检，把标注成本转化为校准曲线。

---

## 6. 评估器有效性验证计划

1. **定位准确率**：答案错误样本上人工标注真实首错步作 ground truth，统计完全命中 / ±1 步 / 未命中；注入样本（已知注入位置）作零成本验证集；
2. **误报率**：答案正确样本中被判过程有问题的全部人工复核，确认真实问题 vs 误报比例；
3. **对抗性验证**：注入"答案对过程错"样本，检验检出率；重点测"将错就错绕回正确答案"和"误用定理歪打正着"两类；
4. **架构对照**：纯 LLM judge vs 多通道仲裁，同验证集比较定位准确率与误报率。

---

## 7. 结果分析计划

1. 基础指标：答案准确率 vs 过程正确率（两者的差距本身就是发现）；
2. 错误类型分布（按难度层分解）；
3. 难度分层分析：找出表现开始明显下降的区间（能力临界点）；
4. 「答案对过程错」统计：各层占比，细分 benign/fatal；
5. 鲁棒性分析：题簇扰动前后的过程正确率变化；
6. 自愈能力分析：可恢复错误占比。

---

## 8. 里程碑计划（2026-08-28 启动，2026-09-11 截止，约两周）

- **8.28–8.30（最小闭环）**：仓库骨架、Hy3 API 调用管线、L1~L3 题集骨架（~60 题）、依赖图分步 prompt，跑通 10 道题的"解答→批改"全流程；
- **8.31–9.3（评估器实现）**：通道 A（SymPy 规则校验）+ 通道 B（LLM 分步裁判）+ 仲裁逻辑；补 L3/L4 题与高考题整理；
- **9.4–9.7（样本构造与验证）**：反例改造、注入样本、强模型生成变体；通道 C（反向验证器）；定位准确率 / 误报率验证实验；
- **9.8–9.11（评测与交付）**：全量评测执行与结果表格、典型 case 归因与临界点分析、分析报告撰写、README 完善、demo 录制；9.11 当天数据集按计划公开发布。

---

## 9. 风险与对策

| 风险 | 对策 |
|---|---|
| 强模型生成题的标准答案有误 | 全部经 SymPy 或人工复核后入库 |
| LLM 裁判自身幻觉导致误报 | 多通道仲裁 + 置信度分层人工抽检 + 架构对照实验 |
| 步骤粒度不对齐影响定位标注 | 依赖图显式分步 + 标注规范明确"语义步"定义 |
| 人工标注工作量大 | 注入样本自带 ground truth；按置信度分层抽检 |
| 样本量不足统计意义弱 | 保证 ≥120 题，L1/L2 正样本充足以支撑误报率估计 |

---

## 10. 自我约束（开源规范）

- 独立新建公开仓库，标注为个人/活动作品，不与官方发布混淆；
- API Key 一律走环境变量/配置文件，提供 `.env.example`，不硬编码；
- 仅通过 API 调用 Hy3，不训练、不微调；
- README 写清项目介绍、运行方式、环境要求。
