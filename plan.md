# 混元 Hy3 数学推理过程评估项目 — 实施规划文档

> 犀牛鸟开源实战任务 · 任务二「可验证场景：过程评估与错误定位」
> 选题方向：数学（竞赛题 / 高考题 / 应用题）
> 本文档为完整实施规划，可直接交付执行。

---

## 1. 项目概述

### 1.1 项目定位

基于腾讯混元 Hy3 大模型，构建一个面向数学领域的「AI 解题 + AI 批改」双层应用：

- **解题层**：调用 Hy3 API，对数学题目输出结构化分步解答（非仅最终答案）
- **过程评估层（核心创新）**：自动判定推理过程正确性、定位首个错误步骤、归类错误类型、识别「答案正确但过程不成立」的样本

最终产出一份系统性刻画 Hy3 数学推理失败模式与能力边界的评测报告。

### 1.2 核心叙事线

> 现有数学 benchmark（如 GSM8K）只判最终答案 → 会系统性高估模型能力（文献：ProcessBench 发现在 OlympiadBench 上答案正确的解答中 32.2% 存在过程错误，Omni-MATH 上高达 51.8%）→ 本项目构建过程级评估器 + 分层题集 → 先证明评估器自身可靠（定位准确率 / 误报率）→ 再用它系统刻画 Hy3 的失败模式与能力临界点 → 归纳典型失败模式。

### 1.3 合规要求（任务书硬性规定）

- 在 GitHub 自行新建公开仓库，不向官方仓库提 PR
- README 写明项目介绍、运行方式、环境要求
- **API Key 不得硬编码**，通过环境变量 / 配置文件传入（提供 `.env.example`）
- 项目命名与 README 标注为「个人 / 活动作品」，避免被误认为腾讯官方发布
- 仅通过 API 调用 Hy3，不训练、不微调

---

## 2. 评测题集构造方案

### 2.1 总体规模与分层配比

总规模 **120~150 题**，四层结构：

| 层级 | 难度定位 | 数量 | 占比 | 主要题型 | 主要用途 |
|---|---|---|---|---|---|
| L1 基础 | 小学/初中应用题 | 30 | ~22% | 四则应用题、比例、一元一次方程 | 测评估器**误报率**（正样本池） |
| L2 中等 | 中考/高一 | 40 | ~30% | 函数、几何计算、数列、概率统计 | 过程问题开始暴露 |
| L3 较难 | 高考压轴/竞赛入门 | 40 | ~30% | 导数综合、圆锥曲线、组合计数、不等式 | **「答案对过程错」高发区，重点观察** |
| L4 高难度 | 竞赛级（AMC/AIME 档） | 30 | ~22% | 数论、组合、多步综合 | 刻画能力临界点 |

每层内部覆盖 3~4 个知识领域（代数/几何/数论组合/概率），题型配比：**开放式计算/证明题 ~70% + 选择题 ~30%**（选择题 25% 蒙对概率，是「答案对过程错」的天然高发题型）。

### 2.2 数据源清单

**L1 — 公开数据集直接抽样：**
- GSM8K（OpenAI，英文小学应用题，带分步解答）抽 15~20 题
- Math23K / Ape210K（中文小学应用题）抽 10~15 题

**L2 — 公开数据集 + 官方试题：**
- MATH 数据集 Level 2-3，约 20 题（自带完整分步解答，直接可用作参考步标注）
- 高考真题（考试院公开试卷）/ AGIEval（已结构化的高考、中考题），15~20 题；高考评分细则天然按步骤给分，可作分步标注依据

**L3 — 竞赛数据集：**
- MATH Level 4-5，10~15 题
- Omni-MATH / OlympiadBench 中低档，15~20 题
- AMC10/12 历年真题（AoPS / MAA 官网，附官方及社区解答）

**L4 — 精选竞赛题：**
- AIME 真题 10~15 题（答案为 0-999 整数，自动校验极方便）
- Omni-MATH / OlympiadBench 高档题 ~10 题
- IMO 短名单 / CMO 真题 3~5 题（能力天花板探测）

**补充构造（任务书要求含难例与反例）：**
1. **改造反例**（15~20 题）：改变条件使常见套路失效（考题意误读）；加入冗余干扰条件（考条件筛选）；设置易猜选项/巧合数值（诱捕「答案对过程错」）
2. **强模型生成**（GPT-4o / Claude 等）：换数字、换情境生成变体题，扩充 L2/L3；**所有生成题的标准答案必须人工或 SymPy 复核后方可入库**
3. **人工注入样本**（10~15 题）：在已知正确分步解的 Step k 注入特定类型错误（计算错、漏条件、误用公式）但保持最终答案不变，记录 `{注入位置, 错误类型}` 作为 ground truth，用于验证评估器的定位准确率与「抓蒙对」能力

### 2.3 入库格式（每题配套资产）

```json
{
  "id": "L3-017",
  "source": "AMC12-2019-B-18",
  "tier": "L3",
  "domain": "组合计数",
  "type": "open",                 
  "problem": "...",
  "answer": "42",
  "answer_check": "numeric",       // numeric / sympy_equiv / choice
  "reference_steps": ["..."],
  "is_adversarial": false,          // 是否为反例/干扰题
  "injected_flaw": null,            // 注入样本: {"step": 3, "type": "计算错误"}
  "cluster_id": "gsm8k-xxx-v2"      // 题簇标识（同题族变体归一簇）
}
```

### 2.4 题簇（cluster）结构

从 GSM8K/MATH 抽 30~40 道种子题，每题用 2~3 种扰动算子生成变体，构成题簇。题簇支持「过程一致性」分析（见 4.5）。

---

## 3. 方法论基础（论文方法迁移）

### 3.1 过程评估范式 — 对齐 ProcessBench（Qwen, arXiv 2412.06559）

- **任务形式化**：给定题目 + 分步解答 S = {s₀,...,s_{n-1}}，输出最早错误步索引；全部正确输出 -1。首个错误之后的步骤即使局部合法也不算对，只评「第一处错」
- **错误类型骨架**：计算/代数错误、逻辑错误（无效推导、无依据假设）、概念错误（误用定理）、完整性错误（漏条件漏论证）——在此基础上扩充为本项目的错误分类体系
- **指标**：错误样本与正确样本分别算准确率，取调和平均 F1，同时惩罚高误报与高漏报
- **可选验证**：在 HuggingFace 公开数据集 `Qwen/ProcessBench` 子集上先跑一遍评估器定位准确率，证明方法可靠后再评 Hy3（评审加分项）

### 3.2 题集与扰动合成方法

| 论文 | 核心思想 | 本项目用法 |
|---|---|---|
| MetaMath（ICLR 2024） | 正向改写 + 反向推理（FOBAR）增强 | 构造 L2/L3 反向变体题，暴露「套路记忆 vs 真理解」 |
| MathScale（微软） | 概念图抽取 → 随机游走生成新题 | 用概念组合深度作为难度分层依据，使分层可量化 |
| Orca-Math（微软） | Agent-Instruct 迭代加难 + 生成-过滤闭环 | 强模型逐层加难生成 L3 题，复核后入库 |
| GSM-Symbolic（Apple 2024） | 模板化符号扰动；插入无关子句（GSM-NoOp） | 鲁棒性反例：题面加无关条件测 Hy3 是否被干扰 |
| GSM-Plus（2024） | 8 种扰动算子 | 作为扰动算子库，每种算子对应一类潜在失败模式 |
| Math-Shepherd（2023） | 蒙特卡洛估计中间步可达性 | 反向用于注入样本构造 + 评估器反向验证通道 |

---

## 4. 评估器设计（在现有方法上的三项核心创新）

### 4.1 创新一：多通道交叉验证仲裁架构

解决单一 LLM-as-judge 波动大、误报高的问题。三通道并行 + 仲裁：

```
解答 Step k
 ├─ 通道 A：符号/规则校验（SymPy 重算等式变换、数值运算）
 │          → 计算类错误给出确定性结论，零幻觉
 ├─ 通道 B：LLM 分步裁判（输入：题目 + 参考解 + 前序步骤判定历史）
 │          → 覆盖概念、逻辑、条件类语义错误
 └─ 通道 C：反向验证器（Math-Shepherd 思想）
            → 从 Step k 结论独立重采样 N 次，看能否到达正确答案
            → 「无法到达」= 高置信「此步可疑」信号
仲裁层：三通道投票；通道冲突时标记为低置信，进入人工复核队列
```

对照实验：混合架构 vs 纯 LLM judge，在验证集上比较误报率，验证架构有效性。

### 4.2 创新二：依赖图显式化的分步格式

解答者输出的每一步必须声明依赖的前序步骤：

```
Step 4: 由 [Step 1, Step 3] 得 ...
```

收益：
- 错误定位从「线性第几步」升级为「依赖图节点」
- **跳步可确定性检测**：引用不存在的前提 / 依赖链断裂
- **循环论证可确定性检测**：依赖图上做环检测
- 成本仅为 prompt 设计，使任务书点名的「跳步、循环论证」两类错误不再依赖裁判语感

### 4.3 创新三：错误区间 + 传播分析

每个错误的输出从单点升级为结构：

```json
{
  "first_error_step": 3,
  "error_type": "条件遗漏",
  "propagation": "fatal",
  "recoverable_at": null
}
```

- `propagation`：fatal（后续全盘皆错）/ benign（笔误但后续自我修正，无害瑕疵）
- `recoverable_at`：若存在本可自我纠正的步骤，指出位置
- 新分析维度：**Hy3 犯错后是「一路错到底」还是「有机会自愈」**——推理鲁棒性的新颖刻画
- 「答案对过程错」细分：benign × 答案正确 = 侥幸过关；fatal × 答案正确 = 纯蒙对/巧合

### 4.4 错误分类体系（在 ProcessBench 四分法上扩充）

1. 题意误读（含被无关条件干扰 — 对应 GSM-NoOp 型反例）
2. 概念/定理误用
3. 计算/代数错误
4. 条件遗漏
5. 跳步推导（依赖图断裂）
6. 循环论证（依赖图成环）
7. 幻觉（捏造题目未给的条件）
8. 格式不符

### 4.5 扩展创新（时间允许则做）

- **自适应对抗采样**：第一轮统计 Hy3 高频错误类型 → 用对应扰动算子定向生成第二批题 → 迭代 2~3 轮，画出「定向题集正确率衰减曲线」，bench 追着模型弱点生长
- **题簇过程一致性指标**：同一题簇内过程正确率的方差；「答案没变但过程崩了」说明靠运气答对，作为 bench 招牌输出指标
- **裁判置信度校准**：裁判输出置信度，按置信度分层人工抽检（低置信 100% 复核，高置信抽 10%），报告校准曲线 — 把人工标注成本转化为方法论贡献

---

## 5. 评估器有效性验证（任务书强制要求）

### 5.1 定位准确率

- 在答案错误的样本上，人工标注「真实首个错误步」作为 ground truth
- 统计评估器定位命中率（完全命中 / 偏差 ±1 步 / 未命中）
- 人工注入样本（已知注入位置）作为零成本定位验证集

### 5.2 误报率

- 在答案正确的样本上，收集评估器判为「过程有问题」的全部样本
- 人工抽检确认：真实问题 vs 误报的比例
- 按裁判置信度分层抽检以降低人工成本

### 5.3 对抗性验证（加分项）

- 用注入样本（答案对过程错）检验评估器能否抓出
- 测试「将错就错绕回正确答案」与「误用定理歪打正着」两类巧合理样本的检出率

### 5.4 架构对照实验

- 纯 LLM judge vs 多通道仲裁架构，在同一验证集上比较定位准确率与误报率

---

## 6. 结果分析输出

1. **基础指标**：最终答案准确率、过程正确率（两者的差距本身是发现）
2. **错误类型分布**：八类错误的占比，按难度层分解
3. **难度分层分析**：指出模型表现开始明显下降的难度区间（能力临界点）
4. **「答案对过程错」统计**：各层占比，细分 benign/fatal
5. **鲁棒性分析**：题簇内扰动（换数字/加无关条件）前后的过程正确率变化
6. **自愈能力分析**：可恢复错误占比与 recoverable_at 分布

---

## 7. 交付物清单

| 交付物 | 内容 |
|---|---|
| 开源仓库 | 应用源码（解题模块 + 过程评估模块）、README、`.env.example`、运行说明 |
| 评测材料 | 分层题集（含标准答案、参考分步解、JSON schema）、答案校验脚本、过程评估脚本 |
| 完整结果 | 答案准确率、过程正确率、错误类型分布、难度分层结果（结果表格） |
| 有效性验证 | 定位准确率与误报率验证数据、人工抽检记录、架构对照实验数据 |
| 分析报告 | 场景选择理由、评估方法设计依据（含文献）、错误分类体系说明、典型 case 归因分析、模型失败模式归纳与能力边界/临界点分析 |
| Demo | ≤2 分钟视频或 GIF，展示「输入题目 → Hy3 分步解答 → 评估器批改并定位错误步」完整流程 |

---

## 8. 实施计划

**阶段一：最小闭环（第 1 周）**
- 搭建仓库骨架、Hy3 API 调用管线（环境变量读 Key）
- GSM8K / MATH / AGIEval 抽样入库 ~60 题（L1~L3 骨架）
- 实现依赖图分步 prompt 模板，跑通 10 道题的「解答 → 批改」全流程

**阶段二：评估器实现（第 2 周）**
- 通道 A（SymPy 规则校验）+ 通道 B（LLM 分步裁判 prompt）+ 仲裁逻辑
- AoPS / OlympiadBench 补 L3/L4 题 ~40 道，高考题整理

**阶段三：样本构造与验证（第 3 周）**
- 反例改造 + 注入样本 + 强模型生成变体 ~30 题，人工核对全部标准答案
- 通道 C（反向验证器）
- 定位准确率 / 误报率验证实验（人工标注 + 抽检）

**阶段四：评测执行与报告（第 4 周）**
- 全量评测执行，输出结果表格
- 典型 case 归因分析、失败模式归纳、临界点分析
- 架构对照实验、分析报告撰写、README 完善、demo 视频录制

---

## 9. 仓库结构建议

```
hunyuan-math-process-eval/
├── README.md                  # 项目介绍(标注个人/活动作品)、运行方式、环境要求
├── .env.example               # HY3_API_KEY=your_key_here
├── requirements.txt
├── data/
│   ├── problems/              # 分层题集 JSON
│   └── schema.md              # 入库格式说明
├── src/
│   ├── solver/                # Hy3 解题模块(依赖图分步 prompt)
│   ├── evaluator/
│   │   ├── channel_sympy.py   # 通道A: 规则校验
│   │   ├── channel_llm.py     # 通道B: LLM分步裁判
│   │   ├── channel_reverse.py # 通道C: 反向验证器
│   │   └── arbiter.py         # 仲裁层
│   └── analysis/              # 统计与图表生成
├── scripts/
│   ├── run_solve.py           # 批量解题
│   ├── run_eval.py            # 批量过程评估
│   └── validate.py            # 有效性验证(定位准确率/误报率)
├── results/                   # 完整结果表格
├── validation/                # 人工标注与抽检记录
└── docs/
    ├── method.md              # 评估方法说明(含文献依据)
    └── report.md              # 分析报告
```

---

## 10. 关键风险与对策

| 风险 | 对策 |
|---|---|
| 强模型生成题的标准答案有误 | 全部经 SymPy 或人工复核后入库 |
| LLM 裁判自身幻觉导致误报 | 多通道仲裁 + 置信度分层人工抽检 + 架构对照实验量化收益 |
| 步骤粒度不对齐影响定位标注 | 依赖图显式分步 + 标注规范明确「语义步」定义 |
| 人工标注工作量大 | 注入样本自带 ground truth；按置信度分层抽检 |
| 样本量不足统计意义弱 | 保证 ≥120 题，L1/L2 正样本充足以支撑误报率估计 |
