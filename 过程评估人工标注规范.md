# 过程评估人工标注规范

> 用途：对 Hy3 真实答错的数学解答进行人工标注，产出「真实首个错误步」ground truth，用于计算评估器的定位准确率；同时对答案正确样本进行过程复核，用于计算误报率。
> 适用项目：混元 Hy3 数学可验证推理与过程评估（犀牛鸟实战任务二）

---

## 1. 标注对象与抽样规则

### 1.1 标注批次

| 批次 | 样本来源 | 样本量 | 用途 |
|---|---|---|---|
| A：真实错题 | Hy3 答案错误的样本（全量约 65 道中抽取） | 25~30 道 | 定位准确率（真实集） |
| B：CBU 样本 | 答案正确但被评估器判为过程有问题的全部样本 | 全量（约 16 道） | 误报率 + CBU 真实性审计 |
| C：答对抽检 | 答案正确且评估器判过程正确的样本 | 随机 20 道 | 误报率（漏检方向） + 评估器阴性可信度 |

抽样要求：批次 A 按难度层分层抽样，各层比例与该层错题占比一致；批次 C 按层均匀抽取（每层 5 道）。

### 1.2 标注产出

每条样本输出一条标注记录（JSONL），字段定义见第 6 节。

---

## 2. 核心概念定义

### 2.1 语义步（step）

以解题输出中解析后的步骤编号为准（`Step 1, Step 2, ...`）。若原始输出未分步或分步明显不合理（如整段为一步），标注员按**语义完整性**先行重切分，并在 `resegmented: true` 中记录，同时给出重切分后的步骤数。

切分原则：**一步 = 一个不可再分的推理动作**（一次代数变形、一次定理应用、一个条件引入、一个中间结论）。只含过渡性文字（"接下来我们计算……"）不含数学内容的行，并入其后续步骤，不单独编号。

### 2.2 首个错误步（first_error_step）

**定义**：推理链条中第一个不成立的步骤。步骤 s_k 不成立，当且仅当满足以下任一条件：

1. **前提合法但推导不合法**：在 s₁…s_{k-1} 均正确且题目条件下，s_k 的结论无法由它们合法推出（计算错误、定理误用、逻辑跳跃）；
2. **引入非法内容**：s_k 引入了题目未给出且无法由前文推出的条件或事实（幻觉/无中生有）；
3. **题意层面的偏离**：s_k 表明对题意的理解已经错误（题意误读），即使其内部计算正确。

**关键规则：首个错误之后的步骤不再单独评价。** 基于错误前提做出的局部合法推导（"将错就错"）不算新错误，也不影响首错步的判定。这与 ProcessBench 的标注约定一致。

### 2.3 定位命中判定

| 判定 | 标准 |
|---|---|
| 完全命中 | 评估器 `first_error_step` == 人工标注值 |
| 偏差 ±1 步 | 两者相差 1（通常源于步骤粒度差异：评估器把含错计算归入前一步或后一步） |
| 未命中 | 相差 ≥2，或评估器判过程完全正确（漏报），或评估器判错但人工认为过程成立（误报） |

报告时三个数字并列：完全命中率、±1 命中率、未命中率。

---

## 3. 典型情形判定细则

### 3.1 一步之内含多个错误

取**在推理顺序上最先发生**的那个错误所在步骤为首错步。若同一步内既有计算错误又有概念错误，`error_type` 记录概念性错误（概念错误在因果上先于计算执行），并在 `error_detail` 中注明计算错误。

### 3.2 跳步的判定标准

判定为跳步需同时满足：

1. 该步结论**无法由前文步骤 + 题目条件 + 中学/竞赛常规定理**直接推出；
2. 省略的中间推导**不是机械性计算**（合并同类项、展开括号、代入数值等默认可省略，不算跳步）。

判定为跳步时，首错步 = 结论"跳"到的那一步（即缺失推导的终点步），不是缺口之前的最后一步。

### 3.3 循环论证

步骤 s_k 的结论被其后续步骤 s_m 用作前提，且 s_m 的结论又被用来支撑 s_k（直接或间接）。首错步 = 循环链条中**先使用待证结论**的那一步。若解答输出带 `depends_on` 依赖标注，以依赖图中的环为准；无依赖标注时需在 `error_detail` 中写明循环路径（如 "s3 → s5 → s3"）。

### 3.4 题意误读

判定基准是**模型建立的数学对象/目标与题面不符**：错设未知量含义、漏看约束条件（"正整数解"看成"整数解"）、答非所问（求面积算了周长）。首错步 = 错误理解首次影响数学内容的那一步（通常是设元/建模步），不是最终答错的那一步。

### 3.5 截断样本

`finish_reason` 为 `length` 或输出明显中断（无 `\boxed{}`、句末不完整、括号未闭合）时：

- 若中断前已有错误：正常标注首错步，另记 `truncated: true`；
- 若中断前无错误：`first_error_step = null`，`error_type = "输出截断"`，`process_valid = false`。截断视为过程不成立，但不计入"推理错误"的错误类型分布统计，单独列报。

### 3.6 答案错误但过程成立（评估口径冲突）

若人工确认过程完全成立但最终答案与标准答案不一致，优先怀疑**答案等价性问题**（分数 vs 小数、未化简、单位差异、`x=5` vs `5`）。标注 `suspected_answer_equivalence: true` 并记录两种形式，交由 answer_checker 修复后重新判定，不记为模型错误。

### 3.7 CBU 样本（批次 B）的专项判定

对"答案对但评估器判过程错"的样本，人工复核后标注 `cbu_audit`：

| 取值 | 含义 |
|---|---|
| `true_positive_fatal` | 过程确实有错，且错误影响结论有效性（蒙对/巧合/错误定理歪打正着）→ 真实 CBU |
| `true_positive_benign` | 过程有瑕疵但属无害笔误，后续自我修正，结论仍被有效支撑 → 真实问题但非"侥幸过关" |
| `false_positive` | 过程实际成立，评估器误报（含 sympy 解析误判）→ 计入误报率 |

### 3.8 多解与非常规解法

模型使用与参考解不同但合法的解法（如几何题用解析法、组合题用生成函数），**不得**因"与参考解不一致"判错。判定依据是推理本身是否成立，参考解仅作辅助。拿不准时标注 `needs_adjudication: true`。

---

## 4. 错误类型标注

`error_type` 从项目十类体系中取值（单选，取因果上最先发生的）：

1. `题意误读` 2. `概念理解错误` 3. `定理/公式误用` 4. `计算错误` 5. `条件遗漏` 6. `跳步推导` 7. `循环论证` 8. `幻觉/无中生有` 9. `单位/格式不符` 10. `其他/无法归类`（另：`输出截断` 单独列报）

判定优先级（同一步多类型候选时）：题意误读 > 幻觉 > 概念理解错误/定理误用 > 条件遗漏 > 循环论证 > 跳步推导 > 计算错误 > 单位/格式不符。

---

## 5. 标注流程与质量控制

### 5.1 流程

1. **盲标**：标注员只看题目 + Hy3 解答 + 标准答案，**不看评估器的判定结果**（避免锚定偏差）。参考分步解折叠，仅在拿不准时展开。
2. **独立双标**：批次 B（CBU 全量）由两人独立标注；批次 A/C 若人力允许也双标，否则单标 + 20% 复检。
3. **仲裁**：双标不一致的样本（首错步不同、或过程成立与否不同）进入仲裁，由第三人或讨论定案，记录 `adjudicated: true`。
4. **一致性统计**：报告标注员间一致率（首错步完全一致的比例）。一致率 < 80% 时回头修订本规范再标。

### 5.2 时间预估

每道错题约 3~8 分钟（L1/L2 快，L4 慢）。批次 A 约 2~3 小时，批次 B 约 1.5 小时，批次 C 约 1.5 小时，合计一天内可完成。

---

## 6. 标注记录格式

```json
{
  "id": "L3-017",
  "batch": "A",
  "annotator": "user1",
  "resegmented": false,
  "n_steps": 7,
  "answer_correct": false,
  "process_valid": false,
  "first_error_step": 3,
  "error_type": "定理/公式误用",
  "error_detail": "Step 3 对非等差数列误用等差中项公式；Step 2 之前均成立",
  "truncated": false,
  "cbu_audit": null,
  "suspected_answer_equivalence": false,
  "needs_adjudication": false,
  "adjudicated": false
}
```

过程成立的样本：`process_valid: true, first_error_step: null, error_type: null`。

---

## 7. 标注完成后的指标计算

- **定位准确率（真实集）**：批次 A 中，评估器判定与人工标注的对照（完全命中 / ±1 / 未命中三档），与注入集的 87% 并列报告
- **误报率**：批次 B 中 `false_positive` 占比
- **CBU 真实性**：批次 B 中 `true_positive_fatal` / `true_positive_benign` 的分布，据此修正报告 8.4 节的 CBU 案例与 10.74% 的数值
- **漏检估计**：批次 C 中人工发现过程有问题但评估器判正确的比例
- **评估器-人工一致性**：可对首错步判定计算 Cohen's κ，作为补充指标
