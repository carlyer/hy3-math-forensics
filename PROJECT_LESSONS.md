# 项目踩坑记录、解决思路与 Insight

> 本文件记录 `hy3-math-eval` 项目从 0 到最终报告过程中，各任务模块遇到的典型困难、思考路径、最终解决方案以及值得保留的 Insight。
> 供后续复现、优化或横向扩展时参考。

---

## 1. 数据层：题集构造与清洗

### 1.1 公开数据集格式五花八门

**困难**：GSM8K、MATH、Math23K、AGIEval、OlympiadBench、Omni-MATH、AIME/AMC 等数据集的字段、答案格式、难度标注完全不同。直接合并会导致后续 answer_checker 无法统一处理。

**思考**：不能把“原始数据”直接当“评测题集”。必须做一次规范化转换，让每道题都具备统一的 `id`、`level`、`problem`、`answer`、`verification` 等字段。

**解决方案**：
- 编写 `dataset/convert_math_data.py`，为每个来源写独立解析器，最终输出统一 schema 的 `problems.jsonl`。
- 在 schema 中显式保留 `source` 和 `construction`，便于追溯污染风险与题源。

**Insight**：
- 数据规范化是后续所有自动评估的前提；前期多花 1 小时统一 schema，后期能省 10 小时调试。
- 保留原始题面与标准答案的“来源说明”，对评审质疑数据污染时非常关键。

### 1.2 答案格式不统一导致误判

**困难**：同一答案可能写成 `16`、`16.0`、`16个`、`\boxed{16}`、`x=5`、`5` 等。早期 `answer_checker` 严格字符串匹配，把大量正确答案判错，造成 L2 层“答案错但过程对”高达 40% 的反常现象。

**思考**：这是 answer_checker 的问题，不是模型的问题。需要先对 gold 和模型输出做“等价形式归一化”，再比较。

**解决方案**：
- 在 `dataset/answer_checker.py` 中增加 `canonicalize_answer`：
  - 剥离 `x=`、`answer is`、`答案是`、单位、百分号、千分位逗号；
  - 尝试分数/小数/百分数互转；
  - 对集合/列表做无序化。
- 修正后 L2 答案正确率从 54.29% 升至 100%，证明 14/16 道“答案错但过程对”实为 gold 标签错误。

**Insight**：
- 在数学评测中，“答案等价”比“字符串相等”重要得多；answer_checker 必须优先做语义/符号归一化。
- 当“答案错但过程对”比例异常高时，优先怀疑答案校验器，而不是 judge。

### 1.3 数据污染风险无法直接证明

**困难**：GSM8K/MATH 等常见数据集极可能已进入 Hy3 训练语料，但无法拿到训练集直接验证。若报告只给最终答案准确率，会被质疑“测的是记忆还是推理”。

**思考**：不能回避污染问题，而应把它变成评测目标的一部分。过程评估天然能暴露“背答案”型解答：答案对但过程跳步、模板化、引用题目未给的信息。

**解决方案**：
- 为每题标注 `contamination_risk: high/medium/low`。
- 设计扰动变体对照实验（`dataset/perturb_problems.py`）：`surface_rewrite` 换人名/情境，`add_noise` 加无关条件。
- 结果发现 `surface_rewrite` 使过程正确率下降 12.5%、CBU 率上升 12.5%，说明部分高分来自表面模板匹配。

**Insight**：
- 污染风险不应被隐藏，而应作为分层分析维度；答案-过程 gap 越大，越可能受污染/记忆影响。
- 扰动变体是低成本但高说服力的“去污染对照”手段。

---

## 2. 解题层：Hy3 客户端与批量生成

### 2.1 本地 vLLM 加载与显存/环境冲突

**困难**：项目复用工作空间 `/path/to/venv`，但本地加载 `hy3-gptq-int4` 需要多卡、显存充足，且不同任务同时运行时容易冲突。

**思考**：优先使用已部署的 OpenAI 兼容 API 服务，本地加载只作为 fallback。

**解决方案**：
- `app/hy3_client.py` 支持 API 模式与本地模式自动切换。
- 在 `.env` 中配置 `HY3_API_BASE`、`HY3_MODEL_NAME`，默认走 API。

**Insight**：
- 评测类项目应尽量通过 API 调用模型，避免把“模型部署”和“评测逻辑”耦合在一起。
- 把 `messages`、`generation_params`、`token_usage`、`finish_reason`、`timestamp`、`model_name` 完整保存到 `solutions_v2.jsonl`，使结果成为可复现的“模型回答快照”。

### 2.2 外部推理模型 API 兼容性问题

**困难**：引入 GPT-5.6-Terra / Gemini-3.5-Flash 作为 judge 时，发现 `gpt-5.6-terra` 不支持 `max_tokens`（需要 `max_completion_tokens`），也不支持 `temperature` / `top_p`。

**思考**：不能把 Hy3 的调用参数直接套到外部模型。需要对推理模型前缀（`o1`、`o3`、`gpt-5.6`）做自动适配。

**解决方案**：
- 在 `app/hy3_client.py` 中增加 `is_reasoning_model` 判断，对推理模型自动 omit `temperature`/`top_p`，并使用 `max_completion_tokens`。
- 对空 `choices` 返回抛出带详情的异常，便于排查网关问题。

**Insight**：
- “OpenAI 兼容”并不等于 100% 参数兼容；多 judge 集成时一定要做参数白名单/模型前缀适配。
- 遇到 API 错误时，把 response 原样打印出来能大幅缩短定位时间。

### 2.3 模型输出解析不稳定

**困难**：Hy3 有时不按 prompt 要求输出 `Step 1`、`\boxed{}`，而是整段连续文本；有时步骤编号重复或混用中文/英文。

**思考**：解析器必须宽容地处理多种分步标记，并提取最终答案。

**解决方案**：
- `app/solution_parser.py` 支持多种分隔模式：显式 `Step/步骤`、中文数字、换行段落。
- 提取 `\boxed{}` 时支持嵌套大括号，找不到时再回退到最后一行。

**Insight**：
- 对模型输出格式的约束越严格，漏判/解析失败越多；解析器应“宽容输入、严格评估”。
- 但宽容解析也带来了副作用：评估器的步骤粒度远小于规范语义步，导致首错步定位在真实样本上大幅失准。

---

## 3. 评估层：规则与符号验证

### 3.1 SymPy 解析 LaTeX 等式时产生大量 SyntaxWarning

**困难**：早期运行 `step_validator.py` 时，控制台被 `'set' object is not callable` 等警告刷屏，影响日志可读性。

**思考**：这不是模型错误，而是 sympy 对 set-like 表达式解析的警告。应尽量减少不必要的解析尝试。

**解决方案**：
- 在 `step_validator.py` 中对表达式做预处理，避免把 `{...}` 集合语法直接喂给 sympy。
- 对无法解析的表达式返回 `null` 而非抛出异常。

**Insight**：
- 符号验证层的原则是“宁可跳过，也不错杀”；无法确定时交给 LLM judge 做语义审查。

### 3.2 链式等式与近似关系被误判为计算错误

**困难**：`step_validator.py` 早期从步骤中“贪婪”提取所有等号，把链式等式 `150 - 50 = 100 = 2*50` 或近似关系 `≈3.14` 错误地拆成不成立的等式对，制造了 16 条假阳性 CBU。

**思考**：规则验证必须保守。链式等式、带余除法、近似/不等关系不应被当成单一等式校验。

**解决方案**：
- 重写 `step_validator.py`：
  - 只校验 LaTeX 块或单行单等号纯文本；
  - 跳过链式等式、带余除法、近似/不等关系；
  - 等式两边分别 sympy 求值后比较容差。
- 重写后单 judge CBU 从 16 条降到 0 条。

**Insight**：
- 规则层假阳性会严重污染 CBU 指标；保守化规则验证比“尽可能多抓错误”更重要。
- CBU 这类“结果对过程错”指标，必须经得起人工审计，否则会被评审质疑。

---

## 4. 评估层：LLM-as-Judge

### 4.1 Judge 输出 JSON 不稳定

**困难**：LLM judge 有时输出额外解释文字，导致 JSON 解析失败；有时字段缺失或格式不对。

**思考**：必须强制 JSON 输出，并在解析失败时做 fallback（如重试或用正则提取）。

**解决方案**：
- 在 `evaluator/llm_judge.py` 的 system prompt 中反复强调“只输出 JSON，不要任何其他文字”。
- 解析失败时记录原始输出，并标记 `parse_error`，不让整条样本被丢弃。

**Insight**：
- 即使 prompt 反复要求，LLM 仍可能输出额外文字；生产级评测必须有“解析失败 → 记录 → 人工抽检”的兜底。
- 解析失败本身也是评估器不稳定性的信号，应在报告中披露。

### 4.2 错误类型分布失衡：大量错误被归为“跳步推导”

**困难**：加 few-shot 后，48 条错误仍被归为“跳步推导”，其余类型几乎为 0。评审会质疑分类体系是否形同虚设。

**思考**：原因有两个：
1. Hy3 在 L3/L4 难题上的真实失败模式确实是“缺少可验证中间环节”；
2. 当前没有显式依赖关系，judge 无法结构性地判定循环论证、条件遗漏等类型。

**解决方案**：
- 在 prompt 中加入每类错误的 few-shot 判例，强制 judge 先排除具体类型再落“跳步”。
- 在报告中如实说明分布不平衡及原因，并把“依赖图显式分步”列为后续计划。

**Insight**：
- 不要为了让分布好看而强行把错误分到不准确的类别；如实报告“跳步兜底”现象比伪造分布更有价值。
- 循环论证、跳步等结构性错误，最终需要依赖图/有向环检测，而不是靠 LLM 语感。

### 4.3 不同裁判对“过程是否成立”门槛差异巨大

**困难**：三 judge 完全一致率仅 48.1%；GPT 与 Gemini 的 Cohen's κ 仅 0.243，说明两者判定标准显著不同。

**思考**：单一 LLM judge 不稳定，必须引入多 judge 投票，并报告裁判间一致性。

**解决方案**：
- 实现 `evaluator/multi_judge.py`：过程正确性取多数票，错误步取中位数，错误类型取众数。
- 编写 `scripts/compute_judge_agreement.py` 计算一致率与 Cohen's κ。

**Insight**：
- LLM-as-judge 的主观差异是真实存在的；不报告一致率的 judge 实验缺乏可信度。
- 多 judge 不是为了“找更狠的裁判”，而是为了平滑个体差异；外部裁判应被视为独立标注员。

---

## 5. 评估层：截断/完整性检测

### 5.1 FrontierMath 样本被截断但评估器未识别

**困难**：FM-v2-011 输出在步骤 7 中间戛然而止，`finish_reason=length`，但早期评估器仍判过程正确。

**思考**：截断是强信号，应覆盖 LLM judge 的结论。

**解决方案**：
- 在 `evaluator/truncation_checker.py` 中实现多层检测：
  - `finish_reason != "stop"`；
  - 输出末尾不是完整句末/数学定界符；
  - 缺少 `\boxed{}`；
  - 括号/LaTeX 定界符未闭合；
  - `completion_tokens` 接近 `max_tokens`。
- 一旦检测到截断，直接判 `process_correct=false`。

**Insight**：
- 截断检测是“零幻觉”的硬规则，应作为评估器最高优先级覆盖层之一。
- 在研究级难题上，模型输出截断是主要失败模式之一，不可忽视。

---

## 6. 评估层：CBU（结果正确但过程不成立）检测

### 6.1 CBU 指标被规则层假阳性严重污染

**困难**：早期单 judge CBU 率 10.74%，但人工审计发现其中 62.5% 是 `step_validator` 等式提取 bug 造成的假阳性。

**思考**：CBU 是任务书点名的核心能力，但必须先把规则层假阳性消除，再谈“真实 CBU”。

**解决方案**：
- 保守化重写 `step_validator`（见 3.2）。
- 引入 GPT 对 CBU 样本做专项审计（`cbu_audit: true_positive_fatal / true_positive_benign / false_positive`）。

**Insight**：
- CBU 检测能力不能只看“检出数量”，必须看“检出的真实性”；假阳性 CBU 会让报告结论站不住脚。
- 多 judge 投票会更严格，但也会引入新的过度严格；需要独立的 CBU 审计来区分 fatal/benign/false_positive。

### 6.2 注入 CBU 样本全部检出，但真实 CBU 极少

**困难**：构造 5 道“答案对但过程错”的合成样本，评估器 100% 检出；但真实多 judge CBU 中仅 12.5% 是致命蒙对。

**思考**：合成注入错误是“模式已知”的错误，不代表真实分布。真实模型更常出现“过程有瑕疵但结论仍被支撑”或“评估器过度严格”。

**解决方案**：
- 在报告中同时报告：合成 CBU 检出率、真实 CBU 审计分布、致命 CBU 比例。
- 不夸大 CBU 率，而是把它作为“答案-过程 gap”的参考指标。

**Insight**：
- 合成集验证的是评估器“能不能抓到明显错误”，真实集验证的是“错误分布什么样”；两者必须并列报告。
- “结果对过程错”的致命案例在真实数据上比例可能很低，但它作为方法论能力必须被验证。

---

## 7. 验证与审计

### 7.1 真实答错题首错步定位命中率低

**困难**：按规范由 GPT 重审 30 道真实错题，评估器首错步 ±1 步命中率仅 7.14%，远低于合成集的 87%。

**思考**：首先区分“过程是否成立”和“首错步位置”。评估器与 GPT 在过程成立与否上 100% 一致，说明二元判断可靠；定位差是因为步骤粒度不对齐。

**解决方案**：
- 在报告中并列报告：
  - 二元一致性 100%；
  - 精确/±1 步命中率 7.14%；
  - 未命中主因是评估器解析步骤粒度 vs 规范语义步差异。
- 把“引入规范化语义步切分/依赖图”列为后续计划。

**Insight**：
- 不要用一个指标否定整个评估器；应分层报告“判断对错”和“定位对错”。
- 步骤粒度是过程评估中容易被忽视但极其关键的工程问题。

### 7.2 人工审计工作量巨大

**困难**：需要审计 L2 gold 标签、16 条 CBU、30 道错题、20 道正确样本、8 道多 judge CBU，人工逐条阅读非常耗时。

**思考**：任务要求“人工抽检”，但并未限定必须由人逐字完成。使用 GPT-5.6-Terra 作为“人工式标注员”，并明确披露，是可行且可复现的方案。

**解决方案**：
- 编写 `scripts/gpt_manual_audit.py`，按《过程评估人工标注规范》批量调用 GPT-5.6-Terra。
- 产出标准 JSONL 标注，便于后续统计与复核。
- 在报告中注明“所有人工复核由 GPT-5.6-Terra 执行，按人工审计标准记录”。

**Insight**：
- 当人工审计规模超过数十条时，用更强模型做“伪人工”标注是性价比极高的方案，但必须在报告中透明披露。
- 伪人工标注的可靠性本身也可以通过双模型交叉或抽样人工复核来验证。

---

## 8. 报告与交付

### 8.1 项目报告与 README 多次出现数字不一致

**困难**：随着修复迭代，报告中出现了旧版数字（56.38%、55.06% 等）与新版数字并存，容易误导读者。

**思考**：每次修改指标后，必须全局搜索旧数字，并确保 README、PROJECT_REPORT、final_report、metrics_report 一致。

**解决方案**：
- 使用 `Grep` 搜索所有旧百分比（56.38、55.06、45.64、51.90、42.28、10.74、14.09 等）。
- 统一替换为最终数字，并保留对旧数字的引用时明确标注“旧版”。
- 通过脚本 `scripts/generate_final_report.py` 从 evaluation JSON 重新生成报告，减少手动维护。

**Insight**：
- 报告中的数字不一致是评审最容易挑的毛病；写完报告后应专门做一次“数字一致性审计”。
- 能用脚本生成的报告尽量用脚本生成，避免手动复制粘贴出错。

### 8.2 交付物清单与目录结构不同步

**困难**：新增脚本/结果文件后，README 的目录结构图和 PROJECT_REPORT 的交付物清单没有同步更新。

**思考**：交付物清单是评审第一眼看到的东西，必须与仓库实际文件一致。

**解决方案**：
- 在 README `scripts/`、`results/`、`audit_outputs/` 块中补充新增文件。
- 在 PROJECT_REPORT 第 11 节加入 `gpt_manual_audit.py` 和三份标注文件。

**Insight**：
- 每次新增文件，都应同步问自己：README 是否体现了？交付物清单是否体现了？

---

## 9. 通用工程与方法论 Insight

1. **先修数据质量，再修模型/评估器**：L2 层的 40% 异常最终被证明是 gold 标签问题，提醒我们数据层 bug 会伪装成模型或评估器 bug。

2. **分层报告比单一指标更有说服力**：最终答案准确率、过程正确率、严格过程正确率、CBU 率、答案错但过程对率，五个指标一起看，才能刻画完整画像。

3. **过程评估的两大能力要分开验证**：
   - “判断过程是否成立”的能力 → 用答对/答错样本的误报/漏判率验证；
   - “定位首错步”的能力 → 用注入集和真实错题分别验证，并承认真实难题上定位更难。

4. **多 judge 不是银弹**：多 judge 能平滑个体差异，但也会提高严格度、制造新的假阳性。必须报告裁判间一致性，并对 CBU 等敏感指标做独立审计。

5. **污染/记忆风险可以转化为研究发现**：与其回避污染，不如用过程评估 + 扰动变体去量化“答案-过程 gap”，把它变成报告的创新点。

6. **保守化是过程评估的生命线**：无论是规则验证还是 LLM judge，宁可漏掉一些轻微错误，也不要制造大量假阳性；假阳性会摧毁 CBU 等核心指标的可信度。

7. **LLM 评测 LLM 必须有元评估**：对 judge 自身要做一致率、κ、重复稳定性测试，否则评测结果缺乏方法论支撑。

---

## 10. 中间迭代实验记录（尝试 → 结果 → 改进）

> 以下记录项目推进过程中，各关键模块的多次尝试、对应结果、以及驱动下一轮改进的思考。保留这些“失败/中间态”有助于理解最终方案为何长成现在这样。

### 10.1 评估器整体迭代

| 迭代 | 改动 | 关键结果（合并题集 158 题） | 思考与下一步 |
|---|---|---|---|
| 初版 | 规则 + sympy + LLM-as-judge 单层评估 | 过程正确率约 62%，CBU 识别不足 | 过程正确率还可以，但 CBU 与截断问题未解决，需要增加专门模块 |
| 迭代 1 | 新增输出截断/未完成检测器；L4 启用 Research 严格 judge prompt | 过程正确率 55.06%，CBU 10.74%；FM-v2-011 截断漏判被修复 | Research prompt 更严格，过程正确率下降、CBU 上升符合预期；但 10.74% CBU 偏高，怀疑有假阳性 |
| 迭代 2 | 新增多 judge 交叉投票框架；适配 内部 AI 网关 的 gpt-5.6-terra / gemini-3.5-flash | 三 judge 下过程正确率 51.90%，CBU 14.09% | 多 judge 更严格，但 CBU 继续升高，需要审计这些 CBU 是真是假 |
| 迭代 3 | 抑制 sympy 解析 set-like 表达式时产生的 SyntaxWarning | 评估日志不再刷屏 | 属于工程清理，不影响指标 |
| 迭代 4 | answer_checker 等价形式归一化；step_validator 保守化；修正 L2 gold 标签；judge prompt 加 few-shot 示例 | 答案正确率 67.79%，Hy3 自审过程正确率 67.09%、CBU 0%；三 judge 过程正确率 61.39%、CBU 5.37% | 数据层 bug 和规则层假阳性消除，但单 judge 仍是 Hy3 自审，存在自评虚高风险 |
| 迭代 5（最终） | LLM-as-judge 默认改为 GPT-5.6-terra 外部裁判，避免 Hy3 自评 | 答案正确率 67.79%，单 judge（GPT）过程正确率 56.33%、CBU 12.08%；三 judge 过程正确率 61.39%、CBU 5.37% | 外部裁判更严格，真实暴露“答案对但过程不成立”；三 judge 因 Hy3 自审拉低严格度，反而比 GPT 单裁判宽松 |

**关键转折**：
- 迭代 1–2 的 CBU 率高企，最初怀疑是 Hy3 真的靠猜；审计后发现 62.5% 是 `step_validator` 等式提取 bug 的假阳性。
- 修复 `step_validator` 后，Hy3 自审 CBU 从 16 条降到 0；但这低估了真实问题。
- 迭代 5 引入外部 GPT 裁判后，CBU 率升至 12.08%，证明 **Hy3 自审对自身解答显著偏宽松**。

### 10.2 answer_checker 的迭代

| 尝试 | 做法 | 结果 | 改进 |
|---|---|---|---|
| 初版 | 字符串精确匹配 | L2 “答案错但过程对”率高达 40%，明显异常 | 增加 `canonicalize_answer`，做前缀剥离、单位/百分号处理、分数/小数互转 |
| 第一次改进 | 数值容差 + 符号等价 | 大部分数值题可判，但 `x=5` vs `5`、`7 米` vs `7` 仍失败 | 继续增加前缀剥离与单位剥离 |
| 当前版 | 综合归一化 + 多种匹配策略 | L2 答案正确率从 54.29% 升至 100%，整体答案正确率从 56.38% 升至 67.79% | 仍有 5 道格式 edge case 未覆盖，已在报告中说明 |

**Insight**：答案校验器的改进顺序应该是：先修等价形式归一化，再修符号/数值匹配，最后处理个别 edge case。

### 10.3 step_validator 的迭代

| 尝试 | 做法 | 结果 | 改进 |
|---|---|---|---|
| 初版 | 从步骤中贪婪提取所有 `=`，左右两边分别 sympy 求值 | 制造 16 条假阳性 CBU；典型如 `150 - 50 = 100` 被拼成 `50 = 150 - 50` 判错 | 重写为保守模式 |
| 保守版 v1 | 只校验 LaTeX 块或单行单等号 | 假阳性大幅下降，但仍有部分链式等式误触发 | 增加链式等式、近似/不等关系跳过 |
| 当前版 | 仅校验明确单一等式；跳过链式、带余除法、近似/不等；非空+重复检测保留 | CBU 从 16 条降到 0；真实计算错误仍可由 LLM judge 捕获 | 与 LLM judge 分工：规则层抓明显错误，语义层抓逻辑错误 |

**Insight**：规则验证层的召回率可以低，但精确率必须高；否则 CBU 这类高敏感度指标会被污染。

### 10.4 L2 层异常排查

| 观察 | 初步假设 | 验证方式 | 结论 |
|---|---|---|---|
| L2 答案正确率 54.29%，过程正确率 85.71% | judge 对 L2 太宽松 | 抽查部分“答案错但过程对”样本 | 发现模型答案实际正确，是 gold 标签或 answer_checker 判错 |
| 进一步审计 16 道 L2 答错题 | gold 标签错误 | 用 GPT-5.6-Terra 复核 | 14/16 道为 gold 标签错误；修正后 L2 答案正确率升至 100% |

**Insight**：当“答案错但过程对”比例异常高时，**优先检查 answer_checker 和 gold 标签**，而不是 judge。

### 10.5 多 judge 实验的迭代

| 尝试 | 配置 | 结果 | 思考 |
|---|---|---|---|
| 单 judge（Hy3） | 仅 Hy3 自审 | 过程正确率 67.09%，CBU 0% | 自审可能偏宽松 |
| 单 judge（GPT） | 仅 GPT-5.6-terra 外部裁判 | 过程正确率 56.33%，CBU 12.08% | 外部裁判更严格，真实暴露问题 |
| 三 judge 简单投票 | Hy3 + GPT-5.6-terra + gemini-3.5-flash，多数决 | 过程正确率 61.39%，CBU 5.37% | 因 Hy3 自审偏宽松，三 judge 反而比 GPT 单裁判宽松 |
| 三 judge + CBU 审计 | 对 8 道多 judge CBU 做 GPT 复核 | false_positive 5、benign 2、fatal 1 | 多 judge 标记的 CBU 中真实致命比例低 |

**Insight**：
- **模型自评会系统性虚高**：Hy3 自审的过程正确率（67.09%）远高于外部 GPT 裁判（56.33%），CBU 从 0% 升至 12.08%。
- **多 judge 不总是更严格**：当其中一个裁判是模型自身时，多 judge 投票可能被拉低严格度，反而不如外部强模型单裁判。
- 若目标是严格刻画过程严谨性，应优先使用外部强模型单裁判；多 judge 更适合平滑多个外部裁判之间的个体差异。

### 10.6 扰动变体对照实验

| 变体类型 | 样本对数 | 原题过程正确率 | 变体过程正确率 | 变化 | 解读 |
|---|---|---|---|---|---|
| `surface_rewrite` | 16 | 62.50% | 50.00% | -12.50% | 换人名/情境/句式使模型更依赖表面模板 |
| `add_noise` | 16 | 62.50% | 62.50% | 0.00% | 加一句无关条件对简单题影响较小 |
| 合计 | 32 | 62.50% | 56.25% | -6.25% | 表面改写会诱发“答案对但过程不成立” |

**思考**：最初以为“换数字”才是最强记忆探测手段；实际运行后发现“表面改写”对过程严谨性的冲击更大，因为模型可能依赖题干表面模板而非抽象数学结构。

**改进方向**：后续应加入“换数字”变体，并将样本量扩展到 100 对以上，获得更稳定的“记忆依赖度”估计。

### 10.7 真实错题定位准确率的迭代

| 阶段 | 方法 | 结果 | 问题 |
|---|---|---|---|
| 早期快速审计 | GPT 复核 30 道真实错题，按评估器步号对比 | 精确命中 38.9%，±1 步 61.1%，无漏判 | 步号对齐依赖评估器自身解析，未按规范语义步切分 |
| 标准化审计 | 按《过程评估人工标注规范》批次 A，GPT 独立切分语义步后对比 | 过程成立与否一致性 100%；首错步 ±1 步仅 7.14% | 评估器解析步骤粒度（可达数十步）与规范语义步（2–11 步）差距巨大 |

**思考**：之前 61.1% 的 ±1 步命中率是高估，因为默认了评估器的步号体系；按规范重审后，定位能力的真实水平暴露出来。

**改进方向**：必须在评估器内部引入与规范对齐的语义步切分，或要求模型显式输出 `depends_on` 依赖图，才能实现可比较的首错步定位。

### 10.8 错误类型分布的迭代

| 阶段 | 做法 | 结果 | 思考 |
|---|---|---|---|
| 初版 | 无 few-shot，judge 自由分类 | 几乎所有错误被归为“其他/无法归类”或“跳步推导” | 分类体系没有起到作用 |
| 当前版 | prompt 中加入每类错误的 few-shot 判例 | 跳步推导 48 次，定理/公式误用 1 次，其他 3 次；条件遗漏/幻觉/循环论证仍为 0 | few-shot 减少了“其他”，但“跳步”仍占绝对多数 |

**思考**：
- 跳步多不等于分类失败，因为 Hy3 在 L3/L4 上的真实失败模式确实是“缺少可验证中间环节”。
- 循环论证、条件遗漏等结构性错误需要依赖图才能准确判定；仅靠 prompt 无法让 LLM 从文本中凭空构建依赖关系。

**改进方向**：
- 依赖图显式分步后，跳步 → 依赖链断裂，循环论证 → 有向环检测。
- 条件遗漏 → 后续结论引用了未声明的前提。

### 10.9 人工审计方法的迭代

| 阶段 | 方法 | 样本量 | 问题 |
|---|---|---|---|
| 早期 | 零散地用 GPT 复核特定异常（L2 gold、16 条 CBU、35 道答对） | 约 60 条 | 缺乏统一标注规范，结果难以复用和统计 |
| 当前 | 按《过程评估人工标注规范》批次 A/B/C，用 `gpt_manual_audit.py` 批量产出标准 JSONL | 58 条 | 步骤粒度差异导致定位指标暴露真实水平，但整体可复现 |

**思考**：人工审计必须从“零散抽查”升级为“按规范批量标注”，否则无法支撑定位准确率、误报率等统计指标。

---

## 11. 后续最想解决的三个问题

1. **依赖图显式分步**：要求模型输出每步的 `depends_on`，把跳步、循环论证从语义判断升级为图算法。
2. **语义步切分对齐**：让评估器的步骤编号与人工标注规范对齐，提升首错步定位准确率。
3. **裁判门槛校准**：通过 few-shot 和置信度分数，降低 GPT/Gemini/Hy3 之间对“过程成立”门槛的差异。
