# Hy3 数学过程评估：设计依据、错误体系、典型案例与能力边界分析

> 本文是 [`PROJECT_REPORT.md`](PROJECT_REPORT.md)（全量项目报告）的配套**分析报告**，聚焦四个问题：
> **为什么这样设计评估方法、错误长什么样、典型样本如何判定、模型能力边界在哪里。**
> 全部数据可从前述仓库文件复核，每处结论均标注出处。项目为「犀牛鸟开源实战任务 · 任务二」个人/活动作品，非腾讯官方发布。

---

## 执行摘要

只看最终答案会系统性高估数学模型能力（ProcessBench 报告 OlympiadBench 上 32.2% 的答对样本存在过程错误，Omni-MATH 上达 51.8%）。本项目为混元 Hy3 构建了 L0–L8 九层混合评估架构，在 418 题主实验集上测得：答案准确率 68.27%，严格过程正确率仅 63.20%，且随难度出现 L1→L2（-22.6pp）、L2→L3（-20.8pp）两道断崖。多组独立证据（GSM-Plus 换数字扰动、L2 层「答案稳、路径飘」、高/中污染组 34.7pp 差距）一致指向：模型在常见题源上的高分含显著记忆红利。评估器本身经注入集、人工复核、多裁判一致性、反向验证器四路元评估校准，其判定结论是「待复核候选集」而非终裁。

### 核心结论速览

| # | 结论 | 关键数据 | 出处 |
|---|---|---|---|
| 1 | 答案正确 ≠ 过程成立 | 418 题 CBU 20 道（5.08%），三裁判口径 37 道（9.39%） | PROJECT_REPORT §8.1/§8.9 |
| 2 | 混合多层架构优于纯 LLM 裁判 | 规则层假阳性曾制造 16 条假 CBU；纯 judge 漏掉截断与循环论证 | PROJECT_LESSONS §3.2/§5.1 |
| 3 | 评估器需元评估才可被采信 | 注入集定位准确率 86.96%；真实错题二元一致率 96.67% | PROJECT_REPORT §7 |
| 4 | 两道能力断崖在 L1→L2 与 L2→L3 | 答案正确率 93.88% → 71.30% → 50.55% | PROJECT_REPORT §8.2 |
| 5 | 记忆依赖有定量指纹 | GSM-Plus 原题 100% → 换数字 80%；L2 答案一致率 100% vs 过程一致率 34.29% | PROJECT_REPORT §8.5/§8.8 |
| 6 | 三裁判揭示更严格的边界 | L4 过程正确率 72.16%（单裁判）→ 34.23%（三裁判） | PROJECT_REPORT §8.9 |

---

## 目录

- [第一章 过程评估方法设计依据](#第一章-过程评估方法设计依据)
  - [1.1 为什么不能只看最终答案](#11-为什么不能只看最终答案)
  - [1.2 九层评估架构的逐层设计动机](#12-九层评估架构的逐层设计动机)
  - [1.3 为什么是「规则 + 符号 + LLM + 算法」混合，而非纯 LLM](#13-为什么是规则--符号--llm--算法混合而非纯-llm)
  - [1.4 设计决策的实证依据](#14-设计决策的实证依据)
  - [1.5 为什么评估器本身需要元评估](#15-为什么评估器本身需要元评估)
- [第二章 错误分类体系说明](#第二章-错误分类体系说明)
  - [2.1 十类错误的定义与判定标准](#21-十类错误的定义与判定标准)
  - [2.2 分类体系的设计原则](#22-分类体系的设计原则)
  - [2.3 算法判定 vs 语义判定的分工](#23-算法判定-vs-语义判定的分工)
  - [2.4 418 题真实分布与「跳步兜底」现象](#24-418-题真实分布与跳步兜底现象)
- [第三章 典型案例分析](#第三章-典型案例分析)
  - [3.1 CBU 案例 I：L3-035（依赖图零幻觉检出）](#31-cbu-案例-il3-035依赖图零幻觉检出)
  - [3.2 CBU 案例 II：L2-015（单点验符号的跳步）](#32-cbu-案例-iil2-015单点验符号的跳步)
  - [3.3 首错步定位准确案例：FM-v2-001](#33-首错步定位准确案例fm-v2-001)
  - [3.4 定位困难案例：EXT-L2-018 与 L3-042](#34-定位困难案例ext-l2-018-与-l3-042)
  - [3.5 「答案错但过程对」案例：L2-022](#35-答案错但过程对案例l2-022)
  - [3.6 截断案例：FM-v2-011](#36-截断案例fm-v2-011)
  - [3.7 记忆/背诵疑似案例：GSM-Plus 簇 gsm8k-test-447](#37-记忆背诵疑似案例gsm-plus-簇-gsm8k-test-447)
  - [3.8 反向验证器：CBU-INJ-001 精确定位与 CBU-INJ-004 漏判](#38-反向验证器cbu-inj-001-精确定位与-cbu-inj-004-漏判)
- [第四章 模型能力边界与临界点分析](#第四章-模型能力边界与临界点分析)
  - [4.1 难度分层数据全景](#41-难度分层数据全景)
  - [4.2 两道断崖的定量分析](#42-两道断崖的定量分析)
  - [4.3 多维边界：题型、验证方式、污染风险](#43-多维边界题型验证方式污染风险)
  - [4.4 记忆依赖的定量证据链](#44-记忆依赖的定量证据链)
  - [4.5 三裁判视角下的更严格边界](#45-三裁判视角下的更严格边界)
  - [4.6 成因推断与下游应用建议](#46-成因推断与下游应用建议)
- [附：数据出处索引](#附数据出处索引)

---

## 第一章 过程评估方法设计依据

### 1.1 为什么不能只看最终答案

**实证一：答案与过程脱节是普遍现象（外部证据）。** ProcessBench 在 OlympiadBench 上发现，答案正确的解答中 32.2% 存在过程错误；Omni-MATH 上该比例高达 51.8%（PROJECT_REPORT §1.2）。这意味着「只看答案」的 benchmark 会系统性高估模型能力，且高估幅度随题目变难而扩大。

**实证二：CBU（Correct But Unjustified）在本项目真实出现。** 418 题主实验集上，外部 GPT-5.6-terra 单裁判标记 20 道「答案正确但过程不成立」（5.08%）；三裁判投票口径下升至 37 道（9.39%），其中 L4 层高达 21.65%（PROJECT_REPORT §8.1、§8.9）。典型案例见第三章 3.1/3.2：答案分毫不差，但中间步骤存在依赖链断裂或未经论证的断言。

**实证三：记忆/污染使「答案正确」进一步失真。** 题集中 high 污染风险组（GSM8K/MATH/AGIEval 等常见数据集，182 题）答案正确率 82.42%，比 medium 组（竞赛/考试题源，121 题，全部为 L3/L4）的 47.71% 高出 34.7 个百分点；GSM-Plus 对照簇中，原题 20/20 全对、仅替换数值即跌至 16/20（PROJECT_REPORT §8.2、§8.5）。若只报答案准确率，无法区分「会推理」与「背过题」。

> **设计推论**：评估的目标函数必须同时包含「答案是否正确」与「过程是否成立」，且两者需要独立的度量通道——这正是「严格过程正确率 = 答案对 ∧ 过程对」这一核心指标的由来。

### 1.2 九层评估架构的逐层设计动机

架构从 L0 到 L8 共九层（PROJECT_REPORT §5，README「过程评估方法」章）。每一层都对应一个被实测证实的**单点失效问题**：

| 层 | 名称 | 方法 | 针对的单点失效 |
|---|---|---|---|
| L0 | 答案层 | `answer_checker` 六种匹配策略 | 最终答案错误；但存在等价性盲区（见 3.5） |
| L1 | 结构层 | 非空、重复/循环复制检测 | 空过程、退化输出 |
| L2 | 符号验证层 | sympy 解析等式并数值重算 | 计算错误——确定性判定、零幻觉 |
| L3 | 语义审查层 | LLM-as-judge（普通/Research 双 prompt + few-shot） | 题意误读、定理误用、条件遗漏、幻觉等语义错误 |
| L4 | 截断检测层 | `finish_reason`、末尾字符、`\boxed{}`、token 用量 | 输出截断被 judge 误判为「过程正确」（FM-v2-011 事件，见 3.6） |
| L5 | 依赖图验证 | 步骤级 `depends_on` 建有向图，查前向引用/环/断链 | 跳步与循环论证靠 LLM 语感判不准（§1.4 实证三） |
| L6 | 回代验证 | 答案代回原题约束独立校验 | L0 等价性争议仲裁；CBU 铁证补强 |
| L7 | 多采样一致性 | temperature>0 采样 N 次，比较答案与路径 | 记忆/背诵（答案稳、路径飘），单次评测不可见 |
| L8 | 反向验证（Math-Shepherd 推理时版） | 步骤前缀续写采样，到达正确答案的经验概率为步骤分 | 不依赖裁判主观的步骤级定位；与 judge 互补（见 3.8） |

两点架构决策值得强调：

1. **优先级覆盖关系**：L4 截断检测、L5 依赖图判定的优先级高于 L3 LLM judge——前者是硬信号，不允许被语义裁判「说服」覆盖（截断即判 `process_correct=false`）。
2. **外部裁判原则**：L3 默认使用外部 GPT-5.6-terra 而非 Hy3 自评。实证：Hy3 自审过程正确率 67.09%、CBU 0%；换外部 GPT 裁判后为 56.33%、CBU 12.08%（158 题子集，PROJECT_LESSONS §10.5）——**模型自评系统性偏宽松**。

### 1.3 为什么是「规则 + 符号 + LLM + 算法」混合，而非纯 LLM

四个通道各有不可替代的能力与各自的失效模式，实测事件是最好的论据：

| 通道 | 不可替代性 | 失效模式（实测） |
|---|---|---|
| 规则/结构 | 零成本、零幻觉 | 贪婪等式提取曾造出 16 条假 CBU（见 1.4 实证一） |
| 符号（sympy） | 计算类错误确定性判定 | 复杂 LaTeX/自然语言无法解析，只能「宁漏勿错」 |
| LLM judge | 唯一能覆盖语义错误（概念、题意、幻觉） | 主观波动大：三裁判完全一致率仅 48.1%（158 题）/ 79.94%（418 题）；自评虚高 |
| 算法（依赖图/回代/采样） | 把结构性错误从零和博弈的语感变成图算法/统计量 | 依赖模型遵守 `depends_on` 输出格式；回代约束提取成功率有限 |

纯 LLM 方案在这张表上每一行都会踩坑：它无法给出计算错误的确定性结论（L1-022 中 judge 判「无错误」而 sympy 检出等式不成立，见 3.1 附注），无法可靠识别截断（FM-v2-011），也无法把循环论证从「语感」变成「有向环」（CBU 注入集中跳步/循环论证两例均被 judge 判为「无错误」，PROJECT_REPORT §7.6）。反过来，纯规则方案对语义错误完全失明。**混合架构的本质是把每类错误交给成本最低且最不易出错的通道。**

### 1.4 设计决策的实证依据

**实证一：step_validator 假阳性事件 → 规则层「宁漏勿错」原则。** 早期符号验证贪婪提取所有等号，把 `150-50=100` 拼成 `50=150-50` 判错，制造了 16 条假 CBU，占当时 CBU 总量的 62.5%（PROJECT_LESSONS §3.2、§6.1）。保守化重写（只校验单一等式、跳过链式/近似/不等关系）后假阳性归零。教训：**CBU 这类高敏感指标会被规则层假阳性彻底摧毁可信度；规则层精确率比召回率重要。**

**实证二：answer_checker 40% 异常事件 → 「先怀疑校验器」。** 初版字符串精确匹配导致 L2 层「答案错但过程对」高达 40%；人工复核 16 道发现 14 道是 gold 标签/校验器归一化问题，修复后 L2 答案正确率从 54.29% 升至 100%（PROJECT_LESSONS §1.2、§10.4）。这直接催生了 L6 回代验证层作为 L0 争议的仲裁通道。

**实证三：裁判一致性数据 → 多裁判与外部裁判的必要性。** 三裁判（Hy3 + GPT-5.6-terra + Gemini-3-flash-preview）对「过程是否成立」的完全一致率：158 题子集 48.10%，418 题全量 79.94%（339 个有效三元组）；两两 Cohen's κ 在 418 全量上收敛到 0.53–0.60 的中等区间。首错步三裁判完全匹配率仅 2.60%（158 题）/ 11.22%（418 题）（PROJECT_REPORT §9.4）。**「是否有错」尚可投票，「错在哪一步」连三个强裁判都无法达成一致**——这既是引入多裁判投票的理由，也是引入 L5/L8 算法定位的理由。

**实证四：单裁判 vs 三裁判的严格度差 → 报告双口径。** 418 题上三裁判投票比单裁判严格：过程正确率 84.45% → 69.86%，L4 层 72.16% → 34.23%（PROJECT_REPORT §8.9）。单裁判在难题上审查过于宽松，因此所有边界结论需要标注裁判口径。

**实证五：定位准确率「虚高」事件 → 语义步对齐。** 早期 ±1 步命中率 61.1% 建立在评估器自身步号体系上；按《过程评估人工标注规范》独立切分语义步重审后，真实命中率仅 7.14%。这直接推动了解题 prompt 要求标注 `depends_on` 与解析器步骤规范化（迭代 6），真实答错题首错步精确命中率最终回升到 51.85%（PROJECT_LESSONS §7.1、§10.7；PROJECT_REPORT §7.3）。

### 1.5 为什么评估器本身需要元评估

LLM 评测 LLM 的结论若要被采信，评估器自身的「测量误差」必须先被量化。本项目从四个正交方向做了元评估（PROJECT_REPORT §7）：

| 元评估维度 | 方法 | 结果 |
|---|---|---|
| 检出能力 | 28 条注入错误验证集（23 错 + 5 对） | 定位准确率 86.96%（20/23），误报率 0%（0/5） |
| 真实分布上的表现 | 30 道真实答错题人工独立标注对比 | 二元一致率 96.67%（29/30）；首错步精确命中 51.85%、±1 步 55.56%；错误类型一致率仅 34.62% |
| 正样本误报 | 55 道答案正确样本抽检 | 漏判率约 3.64%（2/55） |
| CBU 专项 | 9 道合成 CBU 注入 + 8 道真实 CBU 人工审计 | 注入检出 77.78%；真实 CBU 中致命蒙对仅占 1/8 |

三个方法论要点：

1. **合成集与真实集必须并列报告**：合成注入错误是「模式已知」的错误，100% 检出不代表真实能力；真实 CBU 审计显示三裁判标记的 8 道中仅 1 道是致命蒙对（false_positive 5、benign 2、fatal 1，PROJECT_REPORT §7.5）。
2. **「判断对错」与「定位对错」分层验证**：二元判断一致率 96.67% 与首错步命中 51.85% 是两种截然不同的能力，混报会互相掩盖。
3. **裁判本身也要被测量**：单裁判重复稳定性 100%（20 样本 × 3 次），但裁判间 κ 只有 0.53–0.60——波动来自裁判个体差异而非采样随机性，因此投票聚合是有意义的。

---

## 第二章 错误分类体系说明

### 2.1 十类错误的定义与判定标准

分类体系在 ProcessBench 四分法基础上扩充为十类（PROJECT_REPORT §2.3）。完整定义、判定标准与典型特征如下：

| # | 错误类型 | 定义 | 判定标准 | 典型特征（真实样本） |
|---|---|---|---|---|
| 1 | 题意误读 | 对题目条件/目标的语义理解错误 | 步骤中引用的条件与题面不符 | EXT-L1-036 将「十分钟后」误写为「三十分钟后」（数值碰巧不受影响） |
| 2 | 概念理解错误 | 对数学概念的定义/内涵理解错误 | 使用了不成立的概念断言 | EXT-L4-030 将填 9 个不同数字的块误称「3×3 Latin 方阵」；EXT-L2-005 把高的中点误称重心 |
| 3 | 定理/公式误用 | 定理适用条件不满足或公式错套 | 公式形式正确但前提未验证 | FM-v2-001 引用未证明的「密度为 1/2」结论 |
| 4 | 计算错误 | 数值/代数运算错误 | sympy 重算等式两边不等 | L1-022 中 `4/5=0.8` 与 `500×4/5=400` 混用成不成立等式 |
| 5 | 条件遗漏 | 推导依赖了未声明/未验证的前提 | 结论引用了前文未给出的条件 | L2-005 仅验证 x=13 是一个解，未排除其他候选，未证唯一性 |
| 6 | 跳步推导 | 缺少可验证的中间环节 | 步骤间逻辑 gap 无法由已证结论支撑 | L2-015 仅凭两个单点导数符号断定整区间单调性 |
| 7 | 循环论证 | 用结论（或其变体）证明结论 | 依赖图上存在有向环 | 418 题中算法检出 0 例；CBU 注入集 1 例未被 judge 检出 |
| 8 | 幻觉/无中生有 | 引用不存在的定理、数据或条件 | 断言在题面与公理体系中均无来源 | FM-v2-001 的「ord_p(2)=ord_p(3) 仅对有限素数成立」无依据断言 |
| 9 | 单位/格式不符 | 答案单位/格式与要求不符 | answer_checker 归一化后仍不匹配 | 带单位 `150千克` vs 裸数值；选项标签格式差异（L2-035） |
| 10 | 其他/无法归类 | 上述九类均不适用 | 兜底类别 | judge 输出解析失败、复合型错误 |

### 2.2 分类体系的设计原则

1. **互斥性优先于完备性**：每道题只记「首错步」的单一类型。首错之后的错误不进入分布统计（ProcessBench 口径：首错之后的步骤即使局部合法也不算对）。
2. **可操作性**：每类必须能写出可执行的判定规则或 few-shot 判例，否则不进体系。judge prompt 中要求裁判**先排除八个具体类型，再允许落「跳步推导」**，最后才是「其他」（PROJECT_REPORT §8.7 校准段）。
3. **与检测层的显式映射**：每类错误预先指定主检测通道（见 2.3），避免「什么错误都指望 judge」。
4. **诚实报告分布失衡**：不为让分布好看而强行归类——「跳步兜底」现象如实披露并给出成因分析（PROJECT_LESSONS §4.2）。

### 2.3 算法判定 vs 语义判定的分工

| 主判定通道 | 错误类型 | 理由 |
|---|---|---|
| 符号验证（L2） | 计算错误 | 等式重算是确定性的，零幻觉 |
| 依赖图（L5） | 跳步推导（依赖链断裂/前向引用）、循环论证（有向环） | 结构性错误，语感判不准：CBU 注入集中跳步/循环论证两例 judge 均判「无错误」（检出率 0%），而依赖图可在有 `depends_on` 标注时算法检出（PROJECT_REPORT §7.6） |
| 截断检测（L4） | 输出截断 | 硬信号覆盖，不允许 judge 推翻 |
| LLM 语义审查（L3） | 题意误读、概念理解错误、定理误用、条件遗漏、幻觉 | 需要理解题面语义与数学语境，规则做不到 |
| 回代验证（L6）辅助 | 单位/格式不符、答案等价性争议 | 提供独立于 L0 的第二答案判定 |

**为什么结构性错误必须算法化**：循环论证与跳步的判定本质是图问题（是否有环、是否引用未声明前提）。LLM 从线性文本中「凭语感」重建依赖关系的可靠性极低——注入集中这两类检出率为 0%，而依赖图把问题转为 BFS 可达性与有向环检测后变为确定性判定（PROJECT_REPORT §5.7、§8.3）。

### 2.4 418 题真实分布与「跳步兜底」现象

418 题单裁判（GPT-5.6-terra）错误类型分布（PROJECT_REPORT §8.3；`results/evaluation_merged_full_enriched.json`）：

| 错误类型 | 次数 | 占错误样本（65 道）比例 |
|---|---:|---:|
| 无错误 | 353 | —（84.45% 过程正确） |
| 跳步推导 | 20 | 30.8% |
| 计算错误 | 14 | 21.5% |
| 其他/无法归类 | 9 | 13.8% |
| 概念理解错误 | 8 | 12.3% |
| 条件遗漏 | 7 | 10.8% |
| 定理/公式误用 | 4 | 6.2% |
| 题意误读 | 3 | 4.6% |
| 循环论证 | 0 | 0% |

![错误类型分布](assets/figures/error_types.png)

**分布失衡的两层原因**（PROJECT_REPORT §8.7、PROJECT_LESSONS §4.2）：

1. **真实分布确实偏斜**：Hy3 在 L3/L4 难题上的主要失败模式就是「缺少可验证中间环节」——单点验单调性、无依据排除分支、未证唯一性，这些都落为跳步。13 道定位偏差的样本全部来自 L3/L4，也印证难题上的语义边界更模糊。
2. **判定工具的结构性盲区**：few-shot 校准前几乎所有错误落入「其他」；校准后仍堆在「跳步」，因为条件遗漏、循环论证等需要依赖结构才能确诊。依赖图上线后，循环论证的算法检出通道已建立（本轮检出 0 例自环，说明 Hy3 在当前 prompt 下较少出现显式循环论证；跳步中至少有 L3-035 这样的前向引用被图算法而非语感捕获）。

**合理性论证**：跳步占比最高不等于分类体系失效——错误类型一致率仅 34.62%（30 题人工审查）提醒的是「单条样本的类型标签不可尽信」，而非分布趋势不可信。本项目的处理方式是：分布以趋势参考呈现，结构性类型逐步迁移到算法判定，类型标签接受人工审计修正（如 FM-v2-003 经抽检由「概念理解错误」修正为「计算错误」）。

---

## 第三章 典型案例分析

> 以下案例均取自真实评估输出：`results/evaluation_merged_full_enriched.json`（题面/判定）、`results/solutions_merged_full.jsonl`（解答步骤）、`results/reverse_verification_test.json`、`results/validation_real_errors.json`、`validation/frontiermath_spot_check.md`、`dataset/cbu_injection_samples.jsonl`。题面与步骤为原文摘录（有删节）。

### 3.1 CBU 案例 I：L3-035（依赖图零幻觉检出）

**题目摘要**：点 P 在正方形 ABCD 内，∠APB=135°，PC=12，PD=15，求正方形面积。gold：`123+6√119`。

**模型解答关键步骤**：13 步坐标法推导，最终答案 `123 + 6√119` 完全正确。但步骤 4 声明 `depends_on: [4, 5]`：

> Step 4（前向引用处）：「利用 ∠APB=135° 建立关于 x,y,s 的方程……depends_on: [4, 5]」
> Step 5 才是被引用的「将 y² 用 x,s 表示」。

**评估器判定**：依赖图验证检出两处前向引用（步骤 4→5、步骤 6→7）——「Step 4 references future step 5; dependencies must point to earlier steps.」判定 `process_correct=false`，标记 CBU（`correct_but_unjustified: true`）。值得注意的是，**本题 LLM judge 输出解析失败**，若只有 judge 通道，此题将漏判。

**人工复核结论**：在 30 道真实错题标准化审查中，本题评估器与审计员首错步完全一致（均定位于依赖异常处；评估器步号 32 / 语义步对应，类型标签评估器为「跳步推导」、审计员倾向「幻觉/无中生有」，`results/validation_real_errors.json`）。

**案例说明的问题**：①依赖图是 judge 失效时的兜底通道；②「答案对」与「过程合法」可以完全脱节——答案正确但推理的依赖结构自相矛盾；③步骤编号的可靠性本身也是过程质量的一部分（该解答中步骤编号 4/6 重复出现）。

**附注（L1-022，规则 vs judge 分歧）**：小学应用题答案 150 正确，sympy 检出步骤 2「等式不成立：(4)/(5)=0.8 与 500×(4)/(5)=400.0 混用」判 CBU，而 LLM judge 判「无错误」。这是符号层与语义层结论相反时以确定性通道为准的实例。

### 3.2 CBU 案例 II：L2-015（单点验符号的跳步）

**题目摘要**：求 (x⁷+32x²+128)/x³ 在 x>0 时的最小值。gold：48。

**模型解答关键步骤**：

> Step 6：「当 0<x<2 时，取 x=1：f'(1) = -412 < 0，函数递减；当 x>2 时，取 x=3：f'(3) > 0，函数递增。故 x=2 处导数由负变正，为极小值点……」

**评估器判定**：judge 判首错步 6、类型「跳步推导」——「仅分别代入 x=1 和 x=3 检验导数在两个单点的符号，不能仅据此直接断定 f'(x) 在整个区间恒负/恒正……还需说明 f' 连续且除 x=2 外不为零」。答案 48 正确，标记 CBU。

**人工复核结论**：三裁判审计中本题被列为 `true_positive_benign`——跳步真实存在，但「实际可通过因式分解简单补足，结论仍被支撑」（PROJECT_REPORT §7.5）。多采样实验还显示，同题 3 次采样分别用一阶导符号、二阶导凸性、因式分解三种模板验证同一结论（`results/l2_case_analysis.md`）。

**案例说明的问题**：①CBU 内部有 fatal / benign / false_positive 之分，「被判 CBU」≠「蒙对」，需审计分层；②路径漂移的具体形态——模型在等价模板间随机抽取验证方式。

### 3.3 首错步定位准确案例：FM-v2-001

**题目摘要**：FrontierMath v2 研究级数论题（gold：`367707`，模型答 `500000`）。

**评估器判定**：过程错误，首错步定位于断言「ord_p(2)=ord_p(3) 仅对有限个素数成立」处（评估器步号 17；标准化审查口径下评估器与审计员均定位于同一语义步，`results/validation_real_errors.json` 中 `exact_step_match=true`）。

**人工复核结论**（`validation/frontiermath_spot_check.md`）：同意评估器结论——「模型错误地声称 ord_p(2)=ord_p(3) 仅对有限个素数成立，并后续引用未被证明的『密度为 1/2』结论，属于无依据断言。最终答案错误。」类型可归为「定理/公式误用 / 幻觉」。

**案例说明的问题**：在修复步骤索引对齐（迭代 6）后，即使 20+ 步的研究级长链推理，评估器也能与人工标注精确同点定位。这类样本支撑了 51.85% 的精确命中率不是偶然。

### 3.4 定位困难案例：EXT-L2-018 与 L3-042

**EXT-L2-018（采样方差导致定位漂移）**：真实错误为「公式误用」，GPT judge 定位步骤 7；反向验证器定位步骤 4。其前缀分数剧烈波动：

```text
前缀步骤:   1     2     4     5     7     8
分数:     0.25  1.00  0.00  0.25  0.50  0.00
```

（`results/reverse_verification_test.json`；N=4 时单点标准误最高 0.25，`results/reverse_verifier_report.md` §4.1）。两个定位器都说「过程有错」，但对「错在哪」无法收敛。

**L3-042（完全漏检）**：30 道真实错题审查中唯一一道评估器漏检。审计员意见：「按周长枚举并证明 (4,5,6) 是周长最小的可行整数锐角三角形，不能直接推出其面积最小；需额外证明任意周长更大的可行三角形面积都不小于该三角形面积」（首错步 5，跳步推导）。这类「枚举了一个最优性候选但未排除全局」的缺环，是当前 judge 与算法层共同的盲区。

**案例说明的问题**：①定位是比二元判定难一个量级的能力（三裁判首错步完全匹配率仅 11.22%）；②采样式定位需要更大 N（8~16）才能稳定；③「遗漏全局排除论证」型跳步对所有现有通道都构成挑战。

### 3.5 「答案错但过程对」案例：L2-022

**题目摘要**（AGIEval 选择题）：

> A jeep travels a certain distance taking 6 hours in the forward journey. During the return journey, it increased its speed by 12km/hr and took 4 hours. What is the distance? Choices: (A)126km (B)144km (C)127km (D)228km (E)128km

**模型解答**：设去程速度 v，6v = 4(v+12)，解得 v=24，距离 144 km，最终输出 `\boxed{144\text{ km}}`。推导完全正确。

**评估器判定**：`answer_correct=false`——gold 为 `(B)144km`，预测 `144 km` 因空格与选项标签不匹配被判错；过程判正确（judge「无错误」）。

**人工复核结论**：反向验证器复测中同样因 checker 问题「误伤」此题（续写全部到达 144 但分数恒 0），人工复核确认是 **checker 误判**（`results/reverse_verifier_report.md` §3.1）。

**案例说明的问题**：418 题中「答案错但过程对」高达 21.32%（84/394），L3 层甚至 40.66%——其中相当部分是这类格式/等价性伪错误。该队列必须被视为 answer_checker 的待复核集而非模型失败证据，这也是 L6 回代验证存在的意义。

### 3.6 截断案例：FM-v2-011

**题目摘要**：FrontierMath v2 研究级题（递归词函数 F 与对称群 S_n，n=10¹²；无公开答案，manual_check）。

**模型输出**：长篇推理讨论了 F 的递归定义、Cartesian tree、后序遍历等，但**在步骤 7 中间戛然而止**——`finish_reason=length`，输出以非终止字符结尾，无 `\boxed{}`，`completion_tokens` 触及 `max_tokens=4096` 上限。

**评估器判定（历史快照）**：早期评估器判过程正确——LLM judge 对「写到一半但已写部分合理」的输出给出正面结论。人工抽检发现漏判后，新增截断检测层（L4），依据 finish_reason/末尾字符/`\boxed{}`/token 用量四重信号，**强制覆盖 judge 结论**判 `process_correct=false`（`validation/frontiermath_spot_check.md`、PROJECT_LESSONS §5.1）。

**案例说明的问题**：①语义裁判对「未完成的推理」存在系统性宽容；②截断是研究级难题的主要失败模式之一，必须作为最高优先级硬规则；③该事件是「每层解决一个单点失效」架构哲学的直接来源之一。

### 3.7 记忆/背诵疑似案例：GSM-Plus 簇 gsm8k-test-447

同一道 GSM8K 原题的三个变体（`results/evaluation_merged_full_enriched.json`，cluster `gsm8k-test-447`）：

| 变体 | 题号 | 题面摘要 | gold | 模型答案 | 判定 |
|---|---|---|---|---|---|
| 原题 | EXT-L1-031 | Joey 214 分+26；Marcy 225 分+10；领先多少？ | 5 | 5 | ✅ 答案对、过程对 |
| 数值替换 | EXT-L1-032 | Joey 296+17；Marcy 124+39；领先多少？ | 50 | 150 | ❌ 答案错 |
| 干扰插入 | EXT-L1-033 | 原题+「本可多得 5 分但已交牌」「幸运四叶草」 | 5 | -21 | ❌ 答案错、过程错 |

**整体统计**（PROJECT_REPORT §8.5）：20 个簇中，原题答案正确率 100%（20/20），数值替换后 80%（16/20），干扰插入后 95%（19/20）。**仅换数字就造成 20pp 下跌，而插入无关条件只跌 5pp**——模型对「数值改变」远比「文本干扰」敏感，与「记住原题答案而非稳定执行算术」的记忆假说一致。

**必要的诚实声明（本次分析新增复核）**：逐一核查 4 道数值替换「错例」后发现，至少 2 道的 gold 标签本身可疑——EXT-L1-032 模型算得 313-163=150（验算成立）而 gold 标 50；EXT-L1-023 模型算得 27600/240=115 且自行逆运算验证通过，gold 标 140；EXT-L1-020 则是 `\frac{54}{7}\text{ 磅/天}` 含中文单位导致数值解析失败（约 7.71 实际等于 gold 7.71）。这印证了项目自身的教训「先怀疑校验器」（PROJECT_LESSONS §10.4）：**20pp 的记忆指纹在方向上与 L2 多采样证据（§4.4）互相印证，但幅度可能被 gold 噪音高估，精确的「记忆依赖度」需要清洗 gold 后重测**。

### 3.8 反向验证器：CBU-INJ-001 精确定位与 CBU-INJ-004 漏判

**CBU-INJ-001（精确定位）**：题目「计算 5 + 3」，注入错误在步骤 2：

> Step 2：「根据"加法等于乘法"的速算公式，a + b = a × b = 5 × 3 = 15。」
> Step 3：「但题目要求的是普通加法，因此 5 + 3 = 8。」（最终答案 8 正确）

反向验证器前缀分：步骤 1 = 1.00（4/4 续写得 8）→ 步骤 2 = **0.00**（4/4 续写全部走向错误答案 15）→ 分数骤降至 0，**精确定位注入步**，与 judge 一致（`results/reverse_verification_test.json`）。

**CBU-INJ-004（漏判）**：题目「计算 25% of 80」，注入错误在步骤 2：

> Step 2：「先算 80 的 1/5 = 16。」（25% 误用 1/5）
> Step 3：「由于 25% 比 20% 大 5 个百分点，所以再加 4，得到 20。」（打补丁回到正解）

反向验证器三个前缀分全部 1.00——错误步骤逻辑荒谬但**不阻断通往正确答案的路径**，续写模型照样从该前缀走到 20，故判过程可信；GPT judge 反而抓住了步骤 2 的概念错误。

**案例说明的问题**：反向验证器只惩罚「影响结局」的错误（方法性失明），judge 对「结局不变但逻辑矛盾」的语义审查更强——**两者互补而非替代**（PROJECT_REPORT §10.4/§10.5）。这组对照也说明了 L8 存在的意义：为过程判定提供一路不依赖裁判语感、可由采样分布复算的客观信号。

---

## 第四章 模型能力边界与临界点分析

### 4.1 难度分层数据全景

418 题主实验集，单裁判 GPT-5.6-terra（PROJECT_REPORT §8.2；`results/evaluation_merged_full_enriched.json`）：

| 难度 | 题数（可判） | 答案正确率 | 过程正确率 | 严格过程正确率 | CBU 率 | 答案错但过程对率 |
|---|---:|---:|---:|---:|---:|---:|
| L1 基础 | 98（98） | 93.88% | 95.92% | 91.84% | 2.04% | 4.08% |
| L2 中等 | 108（108） | 71.30% | 83.33% | 63.89% | 7.41% | 19.44% |
| L3 较难 | 101（91） | 50.55% | 86.81% | 46.15% | 4.40% | 40.66% |
| L4 高难度 | 111（97） | 55.67% | 72.16% | 49.48% | 6.19% | 22.68% |

![按难度分层的核心指标](assets/figures/level_metrics.png)

两个读表要点：

- **过程正确率不是单调指标**：L3（86.81%）反而高于 L2（83.33%），因为 L3 有 40.66% 的题「答案错但过程被判对」（大量格式/等价性争议）。**随难度单调下降的真实能力指标是严格过程正确率**：91.84% → 63.89% → 46.15% → 49.48%（L4 微升在三裁判口径下消失，见 4.5）。
- **L4 答案正确率（55.67%）高于 L3（50.55%）是构成假象**：L4 含 14 道 AIME 2025/2026 等新题与数值型竞赛题，而 L3 集中了长链证明型题；且 L4 的 CBU 率在三裁判口径下高达 21.65%，单裁判口径的「答对」含金量更低。

### 4.2 两道断崖的定量分析

| 跳变区间 | 答案正确率 | Δ | 严格过程正确率 | Δ |
|---|---:|---:|---:|---:|
| L1 → L2 | 93.88% → 71.30% | **-22.6pp** | 91.84% → 63.89% | **-28.0pp** |
| L2 → L3 | 71.30% → 50.55% | **-20.8pp** | 63.89% → 46.15% | **-17.7pp** |
| L3 → L4 | 50.55% → 55.67% | +5.1pp | 46.15% → 49.48% | +3.3pp |

**为什么断崖在 L1→L2**：L1 全部是「1~3 步显式计算」的应用题（GSM8K/Math23K），而 L2 开始进入多步推理与正确建模（函数、数列、概率、含选择题形式）。同时 L2 恰是污染风险与记忆红利的分界——L2 全量 35 题记忆探测显示过程一致率仅 34.29%（§4.4），即 L2 的 71.30% 里仍有相当比例是「背出来的」。能力门槛与记忆衰减在同一个位置叠加，造成最深的一道断崖。

**为什么第二道在 L2→L3**：L3 要求构造性技巧与 ≥5 个语义步的长推理链（数论、组合、不等式综合）。此时失败模式从「算错/建模错」切换为「推理链中途断裂」——L3 的 CBU 与「答案错但过程对」合计超过 45%，说明模型常在长链中段丢失严格性。13 道首错步定位偏差样本全部来自 L3/L4 也印证了这一点（PROJECT_REPORT §7.3）。

**L3→L4 不再断崖的原因**：L4 的答案正确率含 21.65%（三裁判口径）的 CBU 水分与选择题猜中底噪；若看三裁判严格过程正确率，L3→L4 仍为 39.56% → 34.02% 的下降（§4.5）。能力边界实际在 L3 已经形成，L4 只是延续。

### 4.3 多维边界：题型、验证方式、污染风险

**按问题类型**（PROJECT_REPORT §8.2）：

| 问题类型 | 题数（可判） | 答案正确率 | 过程正确率 |
|---|---:|---:|---:|
| 初等数学 | 225（222） | 79.73% | 89.64% |
| 竞赛数学 | 161（152） | 59.21% | 76.97% |
| 高等数学（MMLU-Pro-Math） | 20（20） | **10.00%** | 85.00% |

高等数学 10% 答案正确率 vs 85% 过程正确率的极端裂口，主要不是能力问题而是**判分问题**：该组答案多为表达式/多部分形式，答案等价性归一化与最终化简差异主导了判错（需要人工复核区分「推导对但形式不符」与「真实错误」）。竞赛数学（59.21%）才是最真实的能力短板类别。

**按验证方式**：可判答案题（394）答案正确率 68.27%；形式化证明题（12）与人工评判题（12）全部 manual_check，过程正确率 83.33% 仅为裁判语义判断，置信度低。

**按污染风险**（PROJECT_REPORT §8.2）：

| 污染风险 | 题数（可判） | 答案正确率 | 过程正确率 |
|---|---:|---:|---:|
| high（GSM8K/MATH/AGIEval 等） | 182（182） | **82.42%** | 88.46% |
| medium（AMC/AIME/Olympiad 等） | 121（109） | **47.71%** | 80.73% |
| low（新题/研究级/扰动变体） | 115（103） | 65.05% | 81.55% |

![污染风险分组对比](assets/figures/contamination_gap.png)

解读需注意构成混杂：medium 组 121 题全部为 L3/L4 难题，其低答案正确率主要是难度效应；但 **high 组过程正确率（88.46%）与 medium（80.73%）只差 7.7pp，答案正确率却差 34.7pp**——答案端红利远大于过程端，这正是「记忆抬高答案、抬不动过程」的预期形态。

### 4.4 记忆依赖的定量证据链

四条独立证据互相印证（PROJECT_REPORT §8.5、§8.7、§8.8）：

1. **GSM-Plus 换数字扰动**：原题 100%（20/20）→ 数值替换 80%（16/20）→ 干扰插入 95%（19/20）。对数值改变的敏感度远高于文本干扰（gold 噪音 caveat 见 §3.7）。
2. **L2 多采样「答案稳、路径飘」**：35 题 × 3 采样，答案一致率 **100%**、自一致率 91.43%，但过程一致率仅 **34.29%**、答案一致但过程不一致率 65.71%。
3. **记忆/模板漂移指标**（`results/l2_memory_metrics_report.md`）：解法类型漂移率 28.57%，依赖图结构漂移率 37.14%（平均边 Jaccard 仅 0.6053），综合记忆漂移标记 **54.29%**（19/35）。典型案例：L2-009 三个样本分别用韦达定理的「直接 -d/a」「首一化 -c」「先陈述公式再代入」三种表述，common_latex_ratio 仅约 8%（`results/l2_case_analysis.md`）。
4. **污染风险关联**：L2 全部 35 题来自 high 污染风险源（MATH/AGIEval）；高污染 + 答案稳定 + 路径在等价模板间漂移，构成「训练语料中存在多种等价讲解、模型随机召回变体」的完整推论链。

![多采样一致性按难度分层](assets/figures/consistency_by_level.png)

**证据链的推论结构**：高污染 → 答案高稳定（背过）；记忆调用 → 路径模板化（不重新构造解法）；模板多版本 → 路径漂移（每次召回不同变体）；过程评估 → 暴露问题（只看答案完全不可见）。

### 4.5 三裁判视角下的更严格边界

同一解答快照，单裁判 vs 三裁判多数投票（PROJECT_REPORT §8.9；`results/evaluation_merged_full_multi_judge.json`）：

| 难度 | 过程正确率（单裁判） | 过程正确率（三裁判） | CBU 率（单裁判） | CBU 率（三裁判） |
|---|---:|---:|---:|---:|
| L1 | 95.92% | 95.92% | 2.04% | 2.04% |
| L2 | 83.33% | 88.89% | 7.41% | 3.70% |
| L3 | 86.81% | 63.37% | 4.40% | 10.99% |
| L4 | 72.16% | **34.23%** | 6.19% | **21.65%** |

![单裁判 vs 三裁判投票](assets/figures/single_vs_multi_judge.png)

三个要点：

1. **严格度差异随难度单调放大**：L1 两口径几乎一致，L4 近乎腰斩。单裁判对高难度题的过程审查过于宽松——难题上只要任一严格裁判发现过程断裂，多数票即不再放行。
2. **三裁判口径下严格过程正确率恢复单调递减**：91.84% → 67.59% → 39.56% → 34.02%，消除了单裁判在 L4 的宽松假象。这应被视为 Hy3 过程可靠性的**下界刻画**。
3. **解读需谨慎**：158 题子集的旧对比（三裁判反而更宽松，CBU 12.08% → 5.37%）方向相反，因为两次对比的单裁判基线来自不同评估批次；本节 418 题对比共享同一解答快照，是同口径结论。小样本、跨批次的多裁判结论不宜外推（PROJECT_REPORT §8.9.3）。

### 4.6 成因推断与下游应用建议

**成因推断**（按证据强度排序）：

1. **记忆/模板依赖是最确定的成因**：四条独立证据（§4.4）一致指向 high 污染题源上的答案高分含记忆红利；模型在高频题型上召回模板而非构造推理。
2. **长链严格性衰减是第二成因**：L3 起的失败集中在「中间环节缺失」而非「知识点不会」——单点验单调性、未证唯一性、无依据排除分支（见 §2.4 分布与 §3.2 案例），属于推理耐力问题。
3. **输出工程约束是第三成因**：截断（FM-v2-011）、步骤编号混乱（L3-035）、答案格式漂移（L2-035）在难题长输出时集中爆发，部分「过程错误」实为生成管理问题。
4. **高等数学/研究级内容的训练覆盖不足**：college_math 10% 答案正确率（即便剔除判分噪音仍极低）、FrontierMath 12 题基本全军覆没，指向知识与技巧的真实缺口。

**对下游应用的建议**：

- **不要用单口径数字描述数学能力**：至少并列答案正确率、严格过程正确率、CBU 率三个指标，并标注裁判口径（单/三裁判差异在 L4 可达 38pp）。
- **高风险题源上的高分必须配扰动对照**：引用 GSM8K/MATH 类分数时，应同步报告换数字变体成绩，并先清洗扰动集 gold（§3.7 的教训）。
- **把 CBU 队列当「待复核候选集」运营**：单裁判标记的 CBU 中约半数经审计为过度严格；落地流水线应是「自动评估 → 置信度排序 → 人工抽检」，而非自动判死。
- **对过程敏感场景（教育批改、自动定理证明辅助）采用三裁判 + 依赖图**：L4 级题目上单裁判会漏掉约一半的过程问题。
- **评估器本身持续元评估**：注入集回归测试（定位 86.96% 基线）、裁判一致性监控（κ 0.53~0.60 基线）应作为评估流水线变更的准入门槛。

---

## 附：数据出处索引

| 本报告数据 | 出处文件 |
|---|---|
| 418 题总体/分层/多维指标、CBU 清单 | `PROJECT_REPORT.md` §8.1–8.5；`results/evaluation_merged_full_enriched.json` |
| 三裁判对比、裁判一致性 | `PROJECT_REPORT.md` §8.9、§9.4；`results/evaluation_merged_full_multi_judge.json` |
| 案例题面与解答步骤 | `results/solutions_merged_full.jsonl` |
| 错误类型分布、依赖图检出 | `PROJECT_REPORT.md` §8.3；`results/evaluation_merged_full_enriched.json` |
| 有效性验证（定位/误报/CBU 审计） | `PROJECT_REPORT.md` §7；`results/validation_real_errors.json`、`results/validation_false_positives.json`、`validation/frontiermath_spot_check.md` |
| 反向验证器实测与局限 | `results/reverse_verifier_report.md`、`results/reverse_verification_test.json` |
| L2 记忆探测与路径漂移案例 | `results/l2_memory_metrics_report.md`、`results/l2_case_analysis.md` |
| 失败迭代与设计教训 | `PROJECT_LESSONS.md` §1–§10 |
| 数据集构成与污染标注 | `dataset/problems_merged_full_stats.md`、`README.md` 数据集章 |
| CBU 注入样本题面 | `dataset/cbu_injection_samples.jsonl` |

> 说明：标注「158 题子集」的数字为中间结果（依赖图/回代/一致性/回代验证等专项实验的口径），418 题全量结论一律以 `results/evaluation_merged_full_enriched.json` 与 `results/evaluation_merged_full_multi_judge.json` 为准。

*报告生成时间：2026-08-31*
